---
import Base from '../../layouts/Base.astro';
import { site, about, stack, learning } from '../../data/site';
import { getTech, techLogo } from '../../data/tech';

// build a branded badge descriptor for a given tech name
function badge(name: string) {
  const logo = techLogo(name);
  const { color } = getTech(name);
  return { name, logo, color };
}

const year = 2026;
const navLinks = [
  { href: '#about', label: 'About' },
  { href: '#stack', label: 'Stack' },
  { href: '#learning', label: 'Learning' },
  { href: '#contact', label: 'Contact' },
];

// editorial stats derived from data (not github cards)
const toolCount = stack.reduce((n, g) => n + g.items.length, 0);
const stats = [
  { value: stack.length, suffix: '', label: 'Stack domains' },
  { value: toolCount, suffix: '+', label: 'Tools & frameworks' },
  { value: year, suffix: '', label: 'Shipping since', raw: true },
];

// marquee track: flatten primary stack into a single editorial stream
const marquee = stack.flatMap((g) => g.items);
---

<Base title="Sihoon Cho — Premium Editorial">
  <div class="pe" data-pe>
    <!-- scroll-driven editorial background -->
    <div class="pe-bg" aria-hidden="true" data-bg>
      <span class="pe-bg__orb pe-bg__orb--a"></span>
      <span class="pe-bg__orb pe-bg__orb--b"></span>
      <span class="pe-bg__grid"></span>
    </div>

    <!-- thin scroll-progress -->
    <div class="pe-progress" aria-hidden="true"><span data-progress></span></div>

    <!-- aria-live region for copy feedback -->
    <p class="pe-sr" role="status" aria-live="polite" data-copy-feedback></p>

    <!-- ───────── header ───────── -->
    <header class="pe-header" data-header role="banner">
      <div class="pe-header__inner">
        <a class="pe-brand" href="#top" aria-label={`${site.name} — 맨 위로`}>
          <span class="pe-brand__mark" aria-hidden="true"></span>
          <span class="pe-brand__name">{site.name}</span>
          <span class="pe-brand__ko" aria-hidden="true">{site.nameKo}</span>
        </a>
        <nav class="pe-nav" aria-label="Primary">
          {navLinks.map((l) => <a class="pe-nav__link" href={l.href}>{l.label}</a>)}
        </nav>
        <a class="pe-cta pe-cta--ghost" href="#contact" data-magnetic>
          <span data-magnetic-inner>연락하기</span>
        </a>
      </div>
    </header>

    <main id="top">
      <!-- ───────── hero ───────── -->
      <section class="pe-hero" aria-labelledby="hero-title">
        <div class="pe-shell pe-hero__grid">
          <div class="pe-hero__main">
            <p class="pe-eyebrow reveal">
              <span class="pe-mono">{site.role}</span>
              <span class="pe-eyebrow__loc pe-mono" aria-hidden="true">— Seoul, KR</span>
            </p>
            <h1 id="hero-title" class="pe-hero__title">
              <span class="pe-line reveal">Reliable</span>
              <span class="pe-line reveal" style="transition-delay:80ms">
                <em class="pe-serif">systems,</em>
              </span>
              <span class="pe-line reveal" style="transition-delay:160ms">shipped</span>
              <span class="pe-line pe-line--accent reveal" style="transition-delay:240ms">&amp; operated.</span>
            </h1>
            <div class="pe-hero__cta reveal" style="transition-delay:320ms">
              <a class="pe-cta pe-cta--solid" href="#contact" data-magnetic>
                <span data-magnetic-inner>연락하기</span>
                <svg class="pe-arrow" viewBox="0 0 16 16" width="14" height="14" aria-hidden="true"
                  ><path d="M3 8h9M8.5 4l4 4-4 4" fill="none" stroke="currentColor" stroke-width="1.6"
                    stroke-linecap="round" stroke-linejoin="round"></path></svg
                >
              </a>
              <a
                class="pe-cta pe-cta--line"
                href={site.links.github}
                target="_blank"
                rel="noopener noreferrer"
                data-magnetic
              >
                <span data-magnetic-inner>GitHub ↗</span>
              </a>
            </div>
          </div>

          <aside class="pe-hero__aside reveal" style="transition-delay:200ms" aria-label="소개">
            <span class="pe-aside__rule" aria-hidden="true"></span>
            <p class="pe-hero__tagline">{site.tagline}</p>
            <p class="pe-hero__tagline pe-hero__tagline--en pe-mono">{site.taglineEn}</p>
            <p class="pe-hero__name-ko pe-mono" aria-hidden="true">{site.nameKo} / {site.name}</p>
          </aside>
        </div>

        <span class="pe-scrollcue pe-mono" aria-hidden="true">
          <span class="pe-scrollcue__line"></span>scroll
        </span>
      </section>

      <!-- ───────── about ───────── -->
      <section class="pe-section" id="about" aria-labelledby="about-title">
        <div class="pe-shell">
          <div class="pe-about__grid">
            <header class="pe-sechead reveal">
              <span class="pe-label pe-mono">About</span>
            </header>
            <div class="pe-about__body">
              <h2 id="about-title" class="pe-lead reveal">{about.lead}</h2>

              <ul class="pe-stats reveal" aria-label="Highlights at a glance">
                {
                  stats.map((s) => (
                    <li class="pe-stat">
                      <span class="pe-stat__num">
                        <span data-count={s.value} data-raw={s.raw ? '1' : '0'}>0</span>
                        {s.suffix}
                      </span>
                      <span class="pe-stat__label pe-mono">{s.label}</span>
                    </li>
                  ))
                }
              </ul>

              <ol class="pe-highlights" aria-label="Approach">
                {
                  about.highlights.map((h, i) => (
                    <li class="pe-hl reveal" style={`transition-delay:${i * 80}ms`}>
                      <span class="pe-hl__bul pe-mono" aria-hidden="true">→</span>
                      <span class="pe-hl__text">{h}</span>
                    </li>
                  ))
                }
              </ol>
            </div>
          </div>
        </div>
      </section>

      <!-- ───────── marquee ───────── -->
      <section class="pe-marquee" aria-hidden="true">
        <div class="pe-marquee__track" data-marquee>
          {
            [...marquee, ...marquee].map((it) => (
              <span class="pe-marquee__item">
                {it}
                <span class="pe-marquee__sep">/</span>
              </span>
            ))
          }
        </div>
      </section>

      <!-- ───────── tech stack ───────── -->
      <section class="pe-section pe-section--top" id="stack" aria-labelledby="stack-title">
        <div class="pe-shell">
          <header class="pe-sechead pe-sechead--row reveal">
            <span class="pe-label pe-mono">Tech Stack</span>
            <h2 id="stack-title" class="pe-sectitle">주력으로 다루는 기술</h2>
          </header>

          <div class="pe-stack">
            {
              stack.map((group, i) => (
                <article class="pe-stack__row reveal" style={`transition-delay:${(i % 4) * 50}ms`}>
                  <div class="pe-stack__cat">
                    <h3 class="pe-stack__catname">{group.category}</h3>
                    <span class="pe-stack__count pe-mono" aria-hidden="true">
                      {String(group.items.length).padStart(2, '0')}
                    </span>
                  </div>
                  <ul class="pe-chips" aria-label={group.category}>
                    {group.items.map((it) => {
                      const b = badge(it);
                      return (
                        <li
                          class="pe-chip"
                          style={`--brand:${b.color};background:${b.color}14;border-color:${b.color}40;`}
                        >
                          {b.logo && (
                            <img
                              class="pe-chip__logo"
                              data-techlogo
                              src={b.logo}
                              alt=""
                              width="16"
                              height="16"
                              loading="lazy"
                            />
                          )}
                          <span class="pe-chip__name">{b.name}</span>
                        </li>
                      );
                    })}
                  </ul>
                </article>
              ))
            }
          </div>
        </div>
      </section>

      <!-- ───────── learning ───────── -->
      <section class="pe-section pe-section--muted" id="learning" aria-labelledby="learning-title">
        <div class="pe-shell">
          <header class="pe-sechead pe-sechead--row reveal">
            <span class="pe-label pe-mono">Learning</span>
            <h2 id="learning-title" class="pe-sectitle pe-sectitle--sm">탐색하고 익히는 중</h2>
          </header>

          <div class="pe-learn">
            {
              learning.map((group, i) => (
                <article class="pe-learn__col reveal" style={`transition-delay:${(i % 3) * 60}ms`}>
                  <h3 class="pe-learn__cat pe-mono">{group.category}</h3>
                  <ul class="pe-learn__items">
                    {group.items.map((it) => {
                      const b = badge(it);
                      return (
                        <li
                          class="pe-learn__badge"
                          style={`--brand:${b.color};background:${b.color}14;border-color:${b.color}40;`}
                        >
                          {b.logo && (
                            <img
                              class="pe-chip__logo"
                              data-techlogo
                              src={b.logo}
                              alt=""
                              width="16"
                              height="16"
                              loading="lazy"
                            />
                          )}
                          <span class="pe-chip__name">{b.name}</span>
                        </li>
                      );
                    })}
                  </ul>
                </article>
              ))
            }
          </div>
        </div>
      </section>

      <!-- ───────── contact ───────── -->
      <section class="pe-section pe-section--top" id="contact" aria-labelledby="contact-title">
        <div class="pe-shell">
          <div class="pe-contact__grid">
            <header class="pe-sechead reveal">
              <span class="pe-label pe-mono">Contact</span>
            </header>
            <div class="pe-contact__body">
              <h2 id="contact-title" class="pe-contact__title reveal">
                같이 만들 거리가 있다면,<br />
                <em class="pe-serif">편하게</em> 연락 주세요.
              </h2>

              <div class="pe-mailrow reveal">
                <a
                  class="pe-cta pe-cta--solid"
                  href="https://mail.google.com/mail/?view=cm&fs=1&to=sihoon.cho.business@gmail.com"
                  target="_blank"
                  rel="noopener noreferrer"
                  data-magnetic
                >
                  <span data-magnetic-inner>Gmail로 보내기 ↗</span>
                </a>
                <button
                  type="button"
                  class="pe-mail"
                  data-copy
                  aria-label={`이메일 주소 복사: ${site.email}`}
                >
                  <span class="pe-mail__addr pe-mono">{site.email}</span>
                  <span class="pe-mail__hint pe-mono" data-copy-label>복사</span>
                </button>
              </div>

              <ul class="pe-social reveal">
                <li>
                  <a class="pe-ulink" href={site.links.github} target="_blank" rel="noopener noreferrer">
                    GitHub <span aria-hidden="true">↗</span>
                  </a>
                </li>
                <li>
                  <a class="pe-ulink" href={site.links.linkedin} target="_blank" rel="noopener noreferrer">
                    LinkedIn <span aria-hidden="true">↗</span>
                  </a>
                </li>
                <li>
                  <a class="pe-ulink" href={site.links.velog} target="_blank" rel="noopener noreferrer">
                    Velog <span aria-hidden="true">↗</span>
                  </a>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </section>
    </main>

    <footer class="pe-footer">
      <div class="pe-shell pe-footer__inner">
        <span class="pe-mono">© {year} {site.name} · {site.nameKo}</span>
        <span class="pe-mono pe-footer__dim">Built with Astro</span>
      </div>
    </footer>
  </div>
</Base>

<style>
  .pe {
    --bg: #f2f1ef;
    --bg-2: #eceae5;
    --ink: #141414;
    --ink-soft: #3a3a38;
    --muted: #6e6c66;
    --faint: #9c9a92;
    --line: rgba(20, 20, 20, 0.12);
    --line-soft: rgba(20, 20, 20, 0.07);
    --accent: #1f4d66; /* deep ink-blue */
    --accent-deep: #163b50;
    --paper: #faf9f7;
    --ease: cubic-bezier(0.16, 1, 0.3, 1);
    --spring: cubic-bezier(0.2, 0.85, 0.25, 1.05);

    --scroll: 0; /* 0 → 1 scroll progress, driven by JS */
    position: relative;
    background: var(--bg);
    color: var(--ink);
    min-height: 100vh;
    font-family: var(--font-sans);
    overflow-x: clip;
  }

  /* ── scroll-driven background ── */
  .pe-bg {
    position: fixed;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    overflow: hidden;
  }
  .pe-bg__orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(70px);
    opacity: 0.5;
    will-change: transform;
  }
  .pe-bg__orb--a {
    width: 46vmax;
    height: 46vmax;
    top: -14vmax;
    right: -10vmax;
    background: radial-gradient(circle at 50% 50%, rgba(31, 77, 102, 0.14), transparent 68%);
    transform: translateY(calc(var(--scroll) * -16vh));
  }
  .pe-bg__orb--b {
    width: 52vmax;
    height: 52vmax;
    bottom: -20vmax;
    left: -16vmax;
    background: radial-gradient(circle at 50% 50%, rgba(150, 120, 90, 0.12), transparent 70%);
    transform: translateY(calc(var(--scroll) * 22vh));
  }
  /* faint editorial grid that drifts + fades as you scroll */
  .pe-bg__grid {
    position: absolute;
    inset: -10% -5%;
    background-image: linear-gradient(rgba(20, 20, 20, 0.04) 1px, transparent 1px),
      linear-gradient(90deg, rgba(20, 20, 20, 0.04) 1px, transparent 1px);
    background-size: 64px 64px;
    opacity: calc(0.5 - var(--scroll) * 0.3);
    transform: translateY(calc(var(--scroll) * -8vh));
  }
  /* keep all real content above the background */
  .pe-header,
  .pe main,
  .pe-footer,
  .pe-progress,
  .pe-sr {
    position: relative;
    z-index: 1;
  }
  .pe-header {
    z-index: 70;
  }

  /* screen-reader-only live region */
  .pe-sr {
    position: absolute;
    width: 1px;
    height: 1px;
    margin: -1px;
    padding: 0;
    overflow: hidden;
    clip: rect(0 0 0 0);
    white-space: nowrap;
    border: 0;
  }

  .pe-mono {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }
  .pe-serif {
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 400;
    letter-spacing: -0.01em;
  }

  /* ── scroll progress ── */
  .pe-progress {
    position: fixed;
    inset: 0 0 auto 0;
    height: 2px;
    z-index: 80;
    background: transparent;
    pointer-events: none;
  }
  .pe-progress span {
    display: block;
    height: 100%;
    width: 100%;
    transform: scaleX(0);
    transform-origin: 0 50%;
    background: var(--accent);
  }

  /* ── shell ── */
  .pe-shell {
    width: 100%;
    max-width: 76rem;
    margin-inline: auto;
    padding-inline: clamp(1.25rem, 5vw, 3.5rem);
    position: relative;
  }

  /* ── header ── */
  .pe-header {
    position: sticky;
    top: 0;
    z-index: 70;
    border-bottom: 1px solid transparent;
    transition: background-color 0.4s var(--ease), border-color 0.4s var(--ease),
      backdrop-filter 0.4s var(--ease);
  }
  .pe-header.is-scrolled {
    background: rgba(242, 241, 239, 0.82);
    backdrop-filter: blur(10px) saturate(120%);
    border-bottom-color: var(--line);
  }
  .pe-header__inner {
    max-width: 76rem;
    margin-inline: auto;
    padding: clamp(1rem, 2vw, 1.3rem) clamp(1.25rem, 5vw, 3.5rem);
    display: flex;
    align-items: center;
    gap: 1.5rem;
    transition: padding 0.4s var(--ease);
  }
  .pe-header.is-scrolled .pe-header__inner {
    padding-top: 0.7rem;
    padding-bottom: 0.7rem;
  }
  .pe-brand {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    text-decoration: none;
    color: var(--ink);
    font-family: var(--font-grotesk);
    font-weight: 600;
    letter-spacing: -0.01em;
  }
  .pe-brand__mark {
    width: 12px;
    height: 12px;
    background: var(--accent);
    transition: transform 0.6s var(--spring), background-color 0.3s var(--ease);
  }
  .pe-brand:hover .pe-brand__mark {
    transform: rotate(45deg) scale(1.15);
    background: var(--accent-deep);
  }
  .pe-brand__ko {
    color: var(--faint);
    font-size: 0.82rem;
    font-weight: 500;
    font-family: var(--font-sans);
  }
  .pe-nav {
    margin-left: auto;
    display: none;
    gap: 0.2rem;
  }
  .pe-nav__link {
    position: relative;
    padding: 0.4rem 0.7rem;
    font-size: 0.82rem;
    color: var(--muted);
    text-decoration: none;
    transition: color 0.25s var(--ease);
  }
  .pe-nav__link::after {
    content: '';
    position: absolute;
    left: 0.7rem;
    right: 0.7rem;
    bottom: 0.15rem;
    height: 1px;
    background: var(--accent);
    transform: scaleX(0);
    transform-origin: 0 50%;
    transition: transform 0.35s var(--spring);
  }
  .pe-nav__link:hover {
    color: var(--ink);
  }
  .pe-nav__link:hover::after {
    transform: scaleX(1);
  }
  @media (min-width: 860px) {
    .pe-nav {
      display: inline-flex;
    }
  }

  /* ── CTAs ── */
  .pe-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.62rem 1.05rem;
    font-size: 0.85rem;
    font-weight: 500;
    text-decoration: none;
    cursor: pointer;
    border-radius: 3px;
    border: 1px solid var(--line);
    color: var(--ink);
    background: transparent;
    will-change: transform;
    transition: transform 0.4s var(--spring), background-color 0.3s var(--ease),
      border-color 0.3s var(--ease), color 0.3s var(--ease), box-shadow 0.3s var(--ease);
  }
  .pe-cta--ghost {
    margin-left: auto;
  }
  @media (min-width: 860px) {
    .pe-cta--ghost {
      margin-left: 0;
    }
  }
  .pe-cta--ghost:hover {
    border-color: var(--ink);
    background: var(--ink);
    color: var(--paper);
  }
  .pe-cta--solid {
    background: var(--ink);
    color: var(--paper);
    border-color: var(--ink);
  }
  .pe-cta--solid:hover {
    background: var(--accent);
    border-color: var(--accent);
    box-shadow: 0 10px 30px -12px rgba(31, 77, 102, 0.6);
  }
  .pe-cta--line:hover {
    border-color: var(--ink);
    background: var(--paper);
  }
  .pe-arrow {
    transition: transform 0.4s var(--spring);
  }
  .pe-cta:hover .pe-arrow {
    transform: translateX(4px);
  }

  /* ── hero ── */
  .pe-hero {
    position: relative;
    padding: clamp(4.5rem, 14vh, 9rem) 0 clamp(3.5rem, 9vh, 6rem);
    min-height: 84vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }
  .pe-hero__grid {
    display: grid;
    gap: clamp(2rem, 5vw, 4rem);
    grid-template-columns: 1fr;
    align-items: end;
  }
  @media (min-width: 900px) {
    .pe-hero__grid {
      grid-template-columns: minmax(0, 1.6fr) minmax(15rem, 0.85fr);
    }
  }
  .pe-eyebrow {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.7rem;
    color: var(--accent);
    margin-bottom: clamp(1.4rem, 3vw, 2.2rem);
  }
  .pe-eyebrow__loc {
    color: var(--faint);
  }
  .pe-hero__title {
    font-family: var(--font-grotesk);
    font-weight: 500;
    line-height: 0.96;
    letter-spacing: -0.04em;
    font-size: clamp(3rem, 11vw, 7.5rem);
    margin: 0;
    color: var(--ink);
  }
  .pe-line {
    display: block;
  }
  .pe-line--accent {
    color: var(--accent);
  }
  .pe-hero__cta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.8rem;
    margin-top: clamp(2rem, 4vw, 3rem);
  }
  .pe-hero__aside {
    position: relative;
    padding-top: 0.4rem;
  }
  .pe-aside__rule {
    display: block;
    width: 2.6rem;
    height: 2px;
    background: var(--accent);
    margin-bottom: 1.3rem;
  }
  .pe-hero__tagline {
    font-size: clamp(1rem, 1.5vw, 1.12rem);
    line-height: 1.62;
    color: var(--ink-soft);
    margin: 0 0 0.9rem;
    max-width: 26rem;
  }
  .pe-hero__tagline--en {
    text-transform: none;
    letter-spacing: 0;
    font-size: 0.82rem;
    line-height: 1.55;
    color: var(--muted);
  }
  .pe-hero__name-ko {
    margin: 1.4rem 0 0;
    color: var(--faint);
    text-transform: none;
    letter-spacing: 0.04em;
  }
  .pe-scrollcue {
    margin-top: clamp(2.5rem, 6vh, 4rem);
    display: inline-flex;
    align-items: center;
    gap: 0.7rem;
    color: var(--faint);
    padding-inline: clamp(1.25rem, 5vw, 3.5rem);
  }
  .pe-scrollcue__line {
    width: 38px;
    height: 1px;
    background: var(--faint);
    position: relative;
    overflow: hidden;
  }
  .pe-scrollcue__line::after {
    content: '';
    position: absolute;
    inset: 0;
    width: 16px;
    background: var(--accent);
    animation: pe-slide 2.2s var(--ease) infinite;
  }
  @keyframes pe-slide {
    from {
      transform: translateX(-16px);
    }
    to {
      transform: translateX(38px);
    }
  }

  /* ── sections ── */
  .pe-section {
    position: relative;
    padding: clamp(3.5rem, 9vh, 7rem) 0;
  }
  .pe-section--top {
    border-top: 1px solid var(--line);
  }
  .pe-section--muted {
    background: var(--bg-2);
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
  }
  .pe-sechead {
    margin-bottom: 2.4rem;
  }
  .pe-sechead--row {
    display: grid;
    gap: 0.7rem 2rem;
    grid-template-columns: 1fr;
    align-items: baseline;
  }
  @media (min-width: 760px) {
    .pe-sechead--row {
      grid-template-columns: 11rem 1fr;
      align-items: start;
    }
  }
  .pe-label {
    display: inline-block;
    color: var(--accent);
    position: relative;
    padding-left: 1.1rem;
  }
  .pe-label::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.32em;
    width: 0.55rem;
    height: 0.55rem;
    background: var(--accent);
  }
  .pe-sectitle {
    font-family: var(--font-grotesk);
    font-weight: 500;
    letter-spacing: -0.03em;
    line-height: 1.05;
    font-size: clamp(2.4rem, 6vw, 4rem);
    margin: 0;
    color: var(--ink);
  }
  .pe-sectitle--sm {
    font-size: clamp(2rem, 5vw, 3.2rem);
  }

  /* ── about ── */
  .pe-about__grid {
    display: grid;
    gap: 0.8rem 2rem;
    grid-template-columns: 1fr;
  }
  @media (min-width: 860px) {
    .pe-about__grid {
      grid-template-columns: 11rem 1fr;
    }
  }
  .pe-lead {
    font-family: var(--font-grotesk);
    font-weight: 400;
    font-size: clamp(1.5rem, 3.4vw, 2.4rem);
    line-height: 1.28;
    letter-spacing: -0.02em;
    margin: 0 0 clamp(2rem, 4vw, 3rem);
    color: var(--ink);
    max-width: 32ch;
  }
  .pe-stats {
    list-style: none;
    margin: 0 0 clamp(2rem, 4vw, 3rem);
    padding: 0;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    border-top: 1px solid var(--line);
    border-left: 1px solid var(--line);
  }
  @media (max-width: 560px) {
    .pe-stats {
      grid-template-columns: 1fr;
    }
  }
  .pe-stat {
    padding: 1.3rem 1.2rem;
    border-right: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }
  .pe-stat__num {
    font-family: var(--font-grotesk);
    font-size: clamp(1.9rem, 5vw, 2.8rem);
    font-weight: 500;
    letter-spacing: -0.03em;
    line-height: 1;
    color: var(--accent);
  }
  .pe-stat__label {
    color: var(--muted);
  }
  .pe-highlights {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 0;
  }
  .pe-hl {
    display: flex;
    gap: 1rem;
    align-items: baseline;
    padding: 1.05rem 0;
    border-top: 1px solid var(--line-soft);
    transition: padding-left 0.4s var(--spring);
  }
  .pe-hl:last-child {
    border-bottom: 1px solid var(--line-soft);
  }
  .pe-hl:hover {
    padding-left: 0.6rem;
  }
  .pe-hl__bul {
    color: var(--accent);
    flex-shrink: 0;
    font-size: 0.8rem;
    transition: transform 0.4s var(--spring);
  }
  .pe-hl:hover .pe-hl__bul {
    transform: translateX(3px);
  }
  .pe-hl__text {
    color: var(--ink-soft);
    line-height: 1.55;
    font-size: clamp(0.95rem, 1.4vw, 1.05rem);
  }

  /* ── marquee ── */
  .pe-marquee {
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
    padding: clamp(0.9rem, 2vw, 1.3rem) 0;
    overflow: hidden;
    background: var(--bg);
  }
  .pe-marquee__track {
    display: inline-flex;
    white-space: nowrap;
    will-change: transform;
    animation: pe-marquee 60s linear infinite;
  }
  .pe-marquee:hover .pe-marquee__track {
    animation-play-state: paused;
  }
  @keyframes pe-marquee {
    from {
      transform: translateX(0);
    }
    to {
      transform: translateX(-50%);
    }
  }
  .pe-marquee__item {
    display: inline-flex;
    align-items: center;
    gap: 1.6rem;
    font-family: var(--font-grotesk);
    font-size: clamp(1.1rem, 2.4vw, 1.6rem);
    font-weight: 400;
    letter-spacing: -0.01em;
    color: var(--ink);
    padding-inline: 0.8rem;
  }
  .pe-marquee__sep {
    color: var(--accent);
    font-family: var(--font-mono);
    font-size: 0.9em;
  }

  /* ── stack ── */
  .pe-stack {
    border-top: 1px solid var(--line);
  }
  .pe-stack__row {
    display: grid;
    gap: 0.8rem 2rem;
    grid-template-columns: 1fr;
    padding: 1.5rem 0;
    border-bottom: 1px solid var(--line-soft);
    transition: background-color 0.3s var(--ease);
  }
  @media (min-width: 760px) {
    .pe-stack__row {
      grid-template-columns: 13rem 1fr;
      align-items: start;
    }
  }
  .pe-stack__row:hover {
    background: linear-gradient(90deg, rgba(31, 77, 102, 0.04), transparent 70%);
  }
  .pe-stack__cat {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 0.7rem;
  }
  @media (min-width: 760px) {
    .pe-stack__cat {
      justify-content: flex-start;
      gap: 0.9rem;
    }
  }
  .pe-stack__catname {
    font-family: var(--font-grotesk);
    font-size: 1.05rem;
    font-weight: 500;
    letter-spacing: -0.01em;
    margin: 0;
    color: var(--ink);
  }
  .pe-stack__count {
    color: var(--faint);
  }
  .pe-chips {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
  }
  .pe-chip {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-family: var(--font-mono);
    font-size: 0.76rem;
    letter-spacing: 0.01em;
    /* label stays dark ink for legibility on light bg, even for pale brands */
    color: var(--ink);
    padding: 0.32rem 0.62rem;
    border: 1px solid var(--line);
    border-bottom: 2px solid var(--brand, var(--accent));
    border-radius: 3px;
    background: var(--paper);
    transition: border-color 0.22s var(--ease), background-color 0.22s var(--ease),
      box-shadow 0.22s var(--ease), transform 0.35s var(--spring);
  }
  .pe-chip__logo {
    display: block;
    flex-shrink: 0;
    width: 16px;
    height: 16px;
    object-fit: contain;
  }
  .pe-chip__name {
    line-height: 1;
  }
  .pe-chip:hover {
    transform: translateY(-2px);
    /* brand-tinted lift; inline style already provides --brand background/border */
    box-shadow: 0 6px 18px -10px var(--brand, var(--accent));
  }

  /* ── learning ── */
  .pe-learn {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    border-top: 1px solid var(--line);
    border-left: 1px solid var(--line);
  }
  @media (min-width: 760px) {
    .pe-learn {
      grid-template-columns: repeat(3, 1fr);
    }
  }
  .pe-learn__col {
    padding: 1.3rem 1.4rem;
    border-right: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
    background: transparent;
    transition: background-color 0.3s var(--ease);
  }
  .pe-learn__col:hover {
    background: var(--paper);
  }
  .pe-learn__cat {
    color: var(--accent);
    margin: 0 0 0.9rem;
  }
  .pe-learn__items {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
  }
  .pe-learn__badge {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-family: var(--font-mono);
    font-size: 0.74rem;
    letter-spacing: 0.01em;
    /* dark ink label keeps pale brand colors legible on the light surface */
    color: var(--ink);
    padding: 0.3rem 0.58rem;
    border: 1px solid var(--line);
    border-left: 2px solid var(--brand, var(--accent));
    border-radius: 3px;
    background: var(--paper);
    transition: border-color 0.22s var(--ease), background-color 0.22s var(--ease),
      box-shadow 0.22s var(--ease), transform 0.35s var(--spring);
  }
  .pe-learn__badge:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 18px -10px var(--brand, var(--accent));
  }

  /* ── contact ── */
  .pe-contact__grid {
    display: grid;
    gap: 0.8rem 2rem;
    grid-template-columns: 1fr;
  }
  @media (min-width: 860px) {
    .pe-contact__grid {
      grid-template-columns: 11rem 1fr;
    }
  }
  .pe-contact__title {
    font-family: var(--font-grotesk);
    font-weight: 500;
    letter-spacing: -0.025em;
    line-height: 1.15;
    font-size: clamp(1.8rem, 5vw, 3.2rem);
    margin: 0 0 2rem;
    color: var(--ink);
  }
  .pe-mailrow {
    display: flex;
    flex-wrap: wrap;
    gap: 0.8rem;
    align-items: center;
  }
  .pe-mail {
    display: inline-flex;
    align-items: center;
    gap: 0.9rem;
    padding: 0.62rem 1rem;
    border: 1px solid var(--line);
    border-radius: 3px;
    background: var(--paper);
    color: var(--ink);
    cursor: pointer;
    transition: border-color 0.3s var(--ease), background-color 0.3s var(--ease);
  }
  .pe-mail:hover {
    border-color: var(--accent);
  }
  .pe-mail__addr {
    text-transform: none;
    letter-spacing: 0;
    font-size: 0.82rem;
  }
  .pe-mail__hint {
    color: var(--accent);
    border-left: 1px solid var(--line);
    padding-left: 0.9rem;
  }
  .pe-social {
    list-style: none;
    margin: 2.4rem 0 0;
    padding: 1.6rem 0 0;
    border-top: 1px solid var(--line);
    display: flex;
    flex-wrap: wrap;
    gap: 1.8rem;
  }
  .pe-ulink {
    color: var(--ink-soft);
    text-decoration: none;
    font-size: 0.9rem;
    position: relative;
    transition: color 0.25s var(--ease);
  }
  .pe-ulink::after {
    content: '';
    position: absolute;
    left: 0;
    bottom: -3px;
    width: 100%;
    height: 1px;
    background: var(--accent);
    transform: scaleX(0);
    transform-origin: 0 50%;
    transition: transform 0.35s var(--spring);
  }
  .pe-ulink:hover {
    color: var(--accent);
  }
  .pe-ulink:hover::after {
    transform: scaleX(1);
  }

  /* ── footer ── */
  .pe-footer {
    border-top: 1px solid var(--line);
    padding: 2.2rem 0;
  }
  .pe-footer__inner {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 0.8rem;
    color: var(--faint);
  }
  .pe-footer__dim {
    color: var(--faint);
  }

  /* ── focus visibility ── */
  .pe a:focus-visible,
  .pe button:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 3px;
    border-radius: 3px;
  }

  @media (prefers-reduced-motion: reduce) {
    .pe-marquee__track,
    .pe-scrollcue__line::after {
      animation: none !important;
    }
    /* freeze scroll-driven background */
    .pe-bg__orb--a,
    .pe-bg__orb--b,
    .pe-bg__grid {
      transform: none !important;
    }
    .pe-bg__grid {
      opacity: 0.35 !important;
    }
  }
</style>

<script>
  (() => {
    const root = document.querySelector<HTMLElement>('[data-pe]');
    if (!root) return;
    const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    // ── graceful fallback: drop any tech logo that fails to load ──
    root.querySelectorAll<HTMLImageElement>('img[data-techlogo]').forEach((i) =>
      i.addEventListener('error', () => i.remove())
    );

    // ── sticky header condense ──
    const header = root.querySelector<HTMLElement>('[data-header]');
    if (header) {
      let ticking = false;
      const update = () => {
        header.classList.toggle('is-scrolled', window.scrollY > 12);
        ticking = false;
      };
      window.addEventListener(
        'scroll',
        () => {
          if (!ticking) {
            window.requestAnimationFrame(update);
            ticking = true;
          }
        },
        { passive: true }
      );
      update();
    }

    // ── scroll progress + hero parallax + scroll-driven background ──
    const bar = root.querySelector<HTMLElement>('[data-progress]');
    const heroTitle = root.querySelector<HTMLElement>('.pe-hero__title');
    if ((bar || heroTitle) && !reduce) {
      let ticking = false;
      const update = () => {
        const max = document.documentElement.scrollHeight - window.innerHeight;
        const p = max > 0 ? Math.min(window.scrollY / max, 1) : 0;
        // drive the editorial background via a CSS variable
        root.style.setProperty('--scroll', p.toFixed(4));
        if (bar) {
          bar.style.transform = `scaleX(${p})`;
        }
        if (heroTitle) {
          const y = Math.min(window.scrollY, 600);
          heroTitle.style.transform = `translateY(${y * 0.08}px)`;
        }
        ticking = false;
      };
      window.addEventListener(
        'scroll',
        () => {
          if (!ticking) {
            window.requestAnimationFrame(update);
            ticking = true;
          }
        },
        { passive: true }
      );
      update();
    }

    // ── magnetic CTAs ──
    if (!reduce) {
      const mags = root.querySelectorAll<HTMLElement>('[data-magnetic]');
      mags.forEach((el) => {
        const inner = el.querySelector<HTMLElement>('[data-magnetic-inner]');
        const strength = 0.3;
        el.addEventListener('pointermove', (e) => {
          const r = el.getBoundingClientRect();
          const x = e.clientX - r.left - r.width / 2;
          const y = e.clientY - r.top - r.height / 2;
          el.style.transform = `translate(${x * strength}px, ${y * strength}px)`;
          if (inner)
            inner.style.transform = `translate(${x * strength * 0.4}px, ${y * strength * 0.4}px)`;
        });
        const reset = () => {
          el.style.transform = '';
          if (inner) inner.style.transform = '';
        };
        el.addEventListener('pointerleave', reset);
        el.addEventListener('blur', reset);
      });
    }

    // ── count-up stats ──
    const counters = root.querySelectorAll<HTMLElement>('[data-count]');
    if (counters.length) {
      const run = (el: HTMLElement) => {
        const target = Number(el.getAttribute('data-count')) || 0;
        const raw = el.getAttribute('data-raw') === '1';
        if (reduce) {
          el.textContent = String(target);
          return;
        }
        const dur = 1100;
        const start = performance.now();
        const tick = (now: number) => {
          const t = Math.min((now - start) / dur, 1);
          const eased = 1 - Math.pow(1 - t, 3);
          const val = Math.round(target * eased);
          el.textContent = raw ? String(val) : String(val);
          if (t < 1) requestAnimationFrame(tick);
        };
        requestAnimationFrame(tick);
      };
      const io = new IntersectionObserver(
        (entries) => {
          for (const e of entries) {
            if (e.isIntersecting) {
              run(e.target as HTMLElement);
              io.unobserve(e.target);
            }
          }
        },
        { threshold: 0.6 }
      );
      counters.forEach((c) => io.observe(c));
    }

    // ── email copy ──
    const EMAIL = 'sihoon.cho.business@gmail.com';
    const copyBtn = root.querySelector<HTMLButtonElement>('[data-copy]');
    const feedback = root.querySelector<HTMLElement>('[data-copy-feedback]');
    const copyLabel = root.querySelector<HTMLElement>('[data-copy-label]');
    if (copyBtn) {
      let timer: number | undefined;
      const show = (msg: string) => {
        if (feedback) feedback.textContent = msg;
        if (copyLabel) copyLabel.textContent = '복사되었습니다!';
        window.clearTimeout(timer);
        timer = window.setTimeout(() => {
          if (copyLabel) copyLabel.textContent = '복사';
          if (feedback) feedback.textContent = '';
        }, 2000);
      };
      const fallback = () => {
        try {
          const ta = document.createElement('textarea');
          ta.value = EMAIL;
          ta.setAttribute('readonly', '');
          ta.style.position = 'absolute';
          ta.style.left = '-9999px';
          document.body.appendChild(ta);
          ta.select();
          document.execCommand('copy');
          document.body.removeChild(ta);
          show('복사되었습니다!');
        } catch {
          show('복사에 실패했습니다. 직접 복사해 주세요.');
        }
      };
      copyBtn.addEventListener('click', async () => {
        try {
          if (navigator.clipboard && window.isSecureContext) {
            await navigator.clipboard.writeText(EMAIL);
            show('복사되었습니다!');
          } else {
            fallback();
          }
        } catch {
          fallback();
        }
      });
    }
  })();
</script>
