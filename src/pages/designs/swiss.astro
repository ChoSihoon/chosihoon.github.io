---
import Base from '../../layouts/Base.astro';
import { site, about, stack, learning } from '../../data/site';

const year = 2026;
const nav = [
  { href: '#about', label: 'About' },
  { href: '#stack', label: 'Stack' },
  { href: '#learning', label: 'Learning' },
  { href: '#contact', label: 'Contact' },
];
// zero-padded ordinals used purely as Swiss compositional numerals (not section labels)
const pad = (n: number) => String(n).padStart(2, '0');
---

<Base title="Sihoon Cho — Swiss Grid">
  <div class="sw" id="top">
    <!-- scroll-driven systematic background — column grid + baseline rules
         that parallax-shift subtly on scroll. Restrained, never flashy. -->
    <div class="sw-bg" aria-hidden="true">
      <div class="sw-shell sw-bg__cols" data-bg-cols>
        {Array.from({ length: 12 }).map(() => <span class="sw-bg__col"></span>)}
      </div>
      <div class="sw-bg__baselines" data-bg-lines></div>
    </div>

    <!-- visible 12-column grid overlay — a deliberate design element (toggle: G) -->
    <div class="sw-grid-overlay" aria-hidden="true">
      <div class="sw-shell sw-grid-overlay__inner">
        {Array.from({ length: 12 }).map(() => <span class="sw-grid-overlay__col"></span>)}
      </div>
    </div>

    <!-- ───────── header ───────── -->
    <header class="sw-header" role="banner">
      <div class="sw-shell sw-header__inner">
        <a class="sw-brand" href="#top" aria-label={`${site.name} — 맨 위로`}>
          <svg class="sw-brand__mark" viewBox="0 0 32 32" width="15" height="15" aria-hidden="true">
            <circle class="sw-brand__tg-b" cx="16" cy="16" r="16" />
            <path
              class="sw-brand__tg-r"
              d="M1.66 8.91 A16 16 0 0 1 30.34 23.09 A8 8 0 0 0 16 16 A8 8 0 0 1 1.66 8.91 Z"
            />
          </svg>
          <span class="sw-brand__name">{site.name}</span>
          <span class="sw-brand__ko">{site.nameKo}</span>
        </a>
        <nav class="sw-nav" aria-label="주요 메뉴">
          {nav.map((l) => (
            <a class="sw-nav__link" href={l.href}>
              <span class="sw-nav__label">{l.label}</span>
            </a>
          ))}
        </nav>
        <span class="sw-header__meta sw-mono" aria-hidden="true">EST. 2026 · SEOUL</span>
      </div>
    </header>

    <main>
      <!-- ───────── hero ───────── -->
      <section class="sw-hero" aria-labelledby="hero-title">
        <div class="sw-shell">
          <div class="sw-hero__top reveal">
            <span class="sw-mono sw-label">{site.role}</span>
            <span class="sw-mono sw-label sw-label--num" aria-hidden="true">{pad(1)}/{pad(5)}</span>
          </div>
          <hr class="sw-rule sw-rule--bold reveal" />
          <h1 id="hero-title" class="sw-hero__title">
            <span class="sw-hero__line reveal">Reliable</span>
            <span class="sw-hero__line reveal">servers,<span class="sw-hero__accent" aria-hidden="true"></span></span>
            <span class="sw-hero__line reveal">operated in<br /><span class="sw-hero__red">the cloud.</span></span>
          </h1>
          <div class="sw-hero__foot">
            <p class="sw-hero__tagline reveal" lang="ko">{site.tagline}</p>
            <p class="sw-hero__tagline sw-hero__tagline--en reveal" lang="en">{site.taglineEn}</p>
          </div>
          <hr class="sw-rule reveal" />
          <div class="sw-hero__index reveal" aria-hidden="true">
            <span class="sw-mono">{site.name}</span>
            <span class="sw-mono">{site.nameKo}</span>
            <span class="sw-mono">{site.role}</span>
            <span class="sw-mono">↓ SCROLL</span>
          </div>
        </div>
      </section>

      <!-- ───────── marquee — single restrained kinetic element ───────── -->
      <div class="sw-marquee" aria-hidden="true">
        <div class="sw-marquee__track" data-marquee>
          {Array.from({ length: 2 }).map(() => (
            <span class="sw-marquee__group">
              <span>Backend</span><span class="sw-marquee__sep">/</span>
              <span>Cloud</span><span class="sw-marquee__sep">/</span>
              <span>DevOps</span><span class="sw-marquee__sep">/</span>
              <span>Java · Spring</span><span class="sw-marquee__sep">/</span>
              <span>Python</span><span class="sw-marquee__sep">/</span>
              <span>AWS · GCP</span><span class="sw-marquee__sep">/</span>
            </span>
          ))}
        </div>
      </div>

      <!-- ───────── about ───────── -->
      <section id="about" class="sw-section" aria-labelledby="about-title">
        <div class="sw-shell sw-section__inner">
          <div class="sw-section__head reveal">
            <h2 id="about-title" class="sw-section__title">About</h2>
            <span class="sw-mono sw-label sw-label--num" aria-hidden="true">{pad(2)}/{pad(5)}</span>
          </div>
          <hr class="sw-rule sw-rule--bold reveal" />
          <div class="sw-about">
            <p class="sw-about__lead reveal" lang="ko">{about.lead}</p>
            <ol class="sw-about__list" role="list">
              {about.highlights.map((h, i) => (
                <li class="sw-about__item reveal" style={`--i:${i}`}>
                  <span class="sw-about__num sw-mono" aria-hidden="true">{pad(i + 1)}</span>
                  <span class="sw-about__text" lang="ko">{h}</span>
                </li>
              ))}
            </ol>
          </div>
        </div>
      </section>

      <!-- ───────── tech stack — typographic index/table ───────── -->
      <section id="stack" class="sw-section sw-section--dark" aria-labelledby="stack-title">
        <div class="sw-shell sw-section__inner">
          <div class="sw-section__head reveal">
            <h2 id="stack-title" class="sw-section__title">Tech Stack</h2>
            <span class="sw-mono sw-label sw-label--num" aria-hidden="true">{pad(3)}/{pad(5)}</span>
          </div>
          <hr class="sw-rule sw-rule--bold sw-rule--inv reveal" />
          <div class="sw-index" role="table" aria-label="주력 기술 스택">
            <div class="sw-index__header sw-mono" role="row" aria-hidden="true">
              <span role="columnheader">No.</span>
              <span role="columnheader">Category</span>
              <span role="columnheader">Items</span>
            </div>
            {stack.map((g, i) => (
              <div class="sw-index__row reveal" role="row" style={`--i:${i}`}>
                <span class="sw-index__num sw-mono" role="cell">{pad(i + 1)}</span>
                <h3 class="sw-index__cat" role="cell">{g.category}</h3>
                <ul class="sw-index__items" role="cell">
                  {g.items.map((it) => (
                    <li class="sw-index__item">{it}</li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        </div>
      </section>

      <!-- ───────── learning — secondary ───────── -->
      <section id="learning" class="sw-section sw-section--muted" aria-labelledby="learning-title">
        <div class="sw-shell sw-section__inner">
          <div class="sw-section__head reveal">
            <h2 id="learning-title" class="sw-section__title sw-section__title--sm">Learning</h2>
            <span class="sw-mono sw-label sw-label--num" aria-hidden="true">{pad(4)}/{pad(5)}</span>
          </div>
          <hr class="sw-rule reveal" />
          <p class="sw-learning__note sw-mono reveal">학습 중 · 탐색 중 · 복습 중</p>
          <div class="sw-learning">
            {learning.map((g, i) => (
              <div class="sw-learning__row reveal" style={`--i:${i}`}>
                <h3 class="sw-learning__cat">{g.category}</h3>
                <ul class="sw-learning__items">
                  {g.items.map((it) => (
                    <li class="sw-learning__item">{it}</li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        </div>
      </section>

      <!-- ───────── contact ───────── -->
      <section id="contact" class="sw-section" aria-labelledby="contact-title">
        <div class="sw-shell sw-section__inner">
          <div class="sw-section__head reveal">
            <h2 id="contact-title" class="sw-section__title">Contact</h2>
            <span class="sw-mono sw-label sw-label--num" aria-hidden="true">{pad(5)}/{pad(5)}</span>
          </div>
          <hr class="sw-rule sw-rule--bold reveal" />
          <div class="sw-contact">
            <div class="sw-contact__main reveal">
              <p class="sw-contact__kicker sw-mono">함께 일하고 싶으신가요?</p>
              <p class="sw-contact__email" data-email>{site.email}</p>
              <div class="sw-contact__actions">
                <a
                  class="sw-btn sw-btn--red"
                  href={`https://mail.google.com/mail/?view=cm&fs=1&to=${site.email}`}
                  target="_blank"
                  rel="noopener noreferrer"
                >
                  <span class="sw-btn__label">Gmail로 보내기</span>
                  <span class="sw-btn__mark" aria-hidden="true">↗</span>
                </a>
                <button type="button" class="sw-btn" data-copy aria-label="이메일 주소 복사하기">
                  <span class="sw-btn__label">이메일 복사</span>
                  <span class="sw-btn__mark" aria-hidden="true">[ ]</span>
                </button>
              </div>
              <p class="sw-contact__feedback sw-mono" data-feedback role="status" aria-live="polite"></p>
            </div>

            <nav class="sw-contact__links reveal" aria-label="외부 링크">
              <a class="sw-link" href={site.links.github} target="_blank" rel="noopener noreferrer">
                <span class="sw-link__idx sw-mono" aria-hidden="true">{pad(1)}</span>
                <span class="sw-link__name">GitHub</span>
                <span class="sw-link__arrow" aria-hidden="true">↗</span>
              </a>
              <a class="sw-link" href={site.links.linkedin} target="_blank" rel="noopener noreferrer">
                <span class="sw-link__idx sw-mono" aria-hidden="true">{pad(2)}</span>
                <span class="sw-link__name">LinkedIn</span>
                <span class="sw-link__arrow" aria-hidden="true">↗</span>
              </a>
              <a class="sw-link" href={site.links.velog} target="_blank" rel="noopener noreferrer">
                <span class="sw-link__idx sw-mono" aria-hidden="true">{pad(3)}</span>
                <span class="sw-link__name">Velog</span>
                <span class="sw-link__arrow" aria-hidden="true">↗</span>
              </a>
            </nav>
          </div>
        </div>
      </section>
    </main>

    <!-- ───────── footer ───────── -->
    <footer class="sw-footer" role="contentinfo">
      <div class="sw-shell sw-footer__inner">
        <hr class="sw-rule sw-rule--bold" />
        <div class="sw-footer__row">
          <span class="sw-footer__copy sw-mono">© {year} Sihoon Cho · 조시훈</span>
          <span class="sw-footer__sys sw-mono" aria-hidden="true">INTERNATIONAL TYPOGRAPHIC STYLE</span>
          <a class="sw-footer__top sw-mono" href="#top">↑ TOP</a>
        </div>
      </div>
    </footer>
  </div>
</Base>

<style>
  /* ─────────────────────────────────────────────
     SWISS GRID — International Typographic Style
     한지(韓紙) ivory + warm black + 태극 적·청 accents
     ───────────────────────────────────────────── */
  .sw {
    --paper: #f3eddf;
    --paper-2: #f8f4ea;
    --ink: #18140d;
    --red: #c0392f;
    --blue: #14467c;
    --hair: rgba(24, 20, 13, 0.16);
    --hair-inv: rgba(248, 244, 234, 0.18);
    --gutter: 24px;
    --margin: clamp(20px, 5vw, 80px);

    /* ── modular typographic scale ──
       A single, deliberate harmonic progression (~1.32 ratio).
       Every type size on the page is drawn from these steps —
       no arbitrary one-off sizes. Müller-Brockmann discipline. */
    --step--1: clamp(0.72rem, 0.7rem + 0.1vw, 0.78rem); /* mono labels / captions */
    --step-0: clamp(0.95rem, 0.92rem + 0.18vw, 1.05rem); /* body */
    --step-1: clamp(1.18rem, 1.05rem + 0.5vw, 1.45rem); /* lead-secondary */
    --step-2: clamp(1.5rem, 1.25rem + 1vw, 2rem); /* sub-display */
    --step-3: clamp(1.9rem, 1.4rem + 1.8vw, 2.75rem); /* display-3 */
    --step-4: clamp(2.4rem, 1.6rem + 3vw, 3.75rem); /* section titles */
    --step-5: clamp(3rem, 2rem + 4vw, 4.75rem); /* large display */
    --step-6: clamp(3.4rem, 2.2rem + 5.4vw, 6.5rem); /* hero — measured, in-grid */

    position: relative;
    background: var(--paper);
    color: var(--ink);
    font-family: var(--font-sans);
    overflow-x: hidden;
    -webkit-font-smoothing: antialiased;
  }

  /* shared 12-col container */
  .sw-shell {
    width: 100%;
    max-width: 1400px;
    margin-inline: auto;
    padding-inline: var(--margin);
  }

  /* ── scroll-driven systematic background ── */
  .sw-bg {
    position: fixed;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    overflow: hidden;
  }
  /* faint always-present 12-column structure; parallaxes vertically on scroll */
  .sw-bg__cols {
    position: absolute;
    inset: -8vh 0;
    height: 116vh;
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    gap: var(--gutter);
    will-change: transform;
  }
  .sw-bg__col {
    border-left: 1px solid rgba(24, 20, 13, 0.045);
    border-right: 1px solid rgba(24, 20, 13, 0.045);
  }
  /* signature 태극 moment: one emphasized column rendered as a faint
     red edge + a faint blue edge — a subtle 적·청 interplay in the grid */
  .sw-bg__col:nth-child(7) {
    border-left-color: rgba(192, 57, 47, 0.08);
    border-right-color: rgba(20, 70, 124, 0.08);
  }
  /* horizontal baseline rules; parallaxes at a different rate for depth */
  .sw-bg__baselines {
    position: absolute;
    inset: -12vh 0;
    height: 124vh;
    background-image: repeating-linear-gradient(
      to bottom,
      rgba(24, 20, 13, 0.035) 0,
      rgba(24, 20, 13, 0.035) 1px,
      transparent 1px,
      transparent 96px
    );
    will-change: transform;
  }

  /* ── visible grid overlay ── */
  .sw-grid-overlay {
    position: fixed;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.6s ease;
  }
  .sw[data-grid='on'] .sw-grid-overlay {
    opacity: 1;
  }
  .sw-grid-overlay__inner {
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    gap: var(--gutter);
    height: 100%;
  }
  .sw-grid-overlay__col {
    border-left: 1px solid rgba(20, 70, 124, 0.16);
    border-right: 1px solid rgba(20, 70, 124, 0.16);
  }

  /* ── rules ── */
  .sw-rule {
    border: 0;
    border-top: 1px solid var(--hair);
    margin: clamp(18px, 3vw, 32px) 0;
  }
  .sw-rule--bold {
    border-top: 3px solid var(--ink);
  }
  .sw-rule--inv {
    border-top-color: var(--paper);
  }

  /* ── mono / labels ── */
  .sw-mono {
    font-family: var(--font-mono);
    font-variant-numeric: tabular-nums;
  }
  .sw-label {
    font-size: 0.72rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  /* ── header ── */
  .sw-header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 50;
    background: color-mix(in srgb, var(--paper) 92%, transparent);
    backdrop-filter: saturate(120%) blur(6px);
    border-bottom: 1px solid var(--hair);
  }
  /* 헤더가 fixed이므로 본문을 헤더 높이만큼 내림 */
  .sw main {
    padding-top: 64px;
  }
  .sw-header__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--gutter);
    height: 64px;
  }
  .sw-brand {
    display: inline-flex;
    align-items: baseline;
    gap: 10px;
    text-decoration: none;
    color: var(--ink);
  }
  .sw-brand__mark {
    width: 15px;
    height: 15px;
    align-self: center;
    transition: transform 0.45s cubic-bezier(0.16, 1, 0.3, 1);
  }
  .sw-brand__tg-r {
    fill: var(--red);
  }
  .sw-brand__tg-b {
    fill: var(--blue);
  }
  .sw-brand:hover .sw-brand__mark {
    transform: scale(1.08);
  }
  .sw-brand__name {
    font-family: var(--font-grotesk);
    font-weight: 700;
    font-size: 1rem;
    letter-spacing: -0.01em;
  }
  .sw-brand__ko {
    font-size: 0.82rem;
    color: rgba(24, 20, 13, 0.55);
  }
  .sw-nav {
    display: none;
    gap: clamp(14px, 2vw, 30px);
  }
  @media (min-width: 720px) {
    .sw-nav {
      display: flex;
    }
  }
  .sw-nav__link {
    position: relative;
    text-decoration: none;
    color: var(--ink);
    font-size: 0.82rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    padding-bottom: 2px;
  }
  .sw-nav__link::after {
    content: '';
    position: absolute;
    left: 0;
    bottom: 0;
    width: 100%;
    height: 2px;
    background: var(--red);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.28s cubic-bezier(0.16, 1, 0.3, 1);
  }
  .sw-nav__link:hover::after,
  .sw-nav__link:focus-visible::after {
    transform: scaleX(1);
  }
  .sw-header__meta {
    display: none;
    font-size: 0.68rem;
    letter-spacing: 0.12em;
    color: rgba(24, 20, 13, 0.5);
  }
  @media (min-width: 980px) {
    .sw-header__meta {
      display: inline;
    }
  }

  /* ── hero ── */
  .sw-hero {
    position: relative;
    z-index: 1;
    padding-top: clamp(48px, 9vw, 120px);
    padding-bottom: clamp(28px, 5vw, 56px);
  }
  .sw-hero__top {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    color: rgba(24, 20, 13, 0.7);
  }
  /* compositional index numerals — structural marker → 태극 청 */
  .sw-label--num {
    color: var(--red);
  }
  /* 다크 패널(Tech Stack)에서는 청 번호를 한지쪽으로 끌어올려 가시성 확보 */
  .sw-section--dark .sw-label--num {
    color: color-mix(in srgb, var(--red) 55%, var(--paper));
  }
  .sw-hero__title {
    font-family: var(--font-grotesk);
    font-weight: 700;
    line-height: 0.98;
    letter-spacing: -0.03em;
    /* disciplined display step of the modular scale — measured, not screen-filling */
    font-size: var(--step-6);
    margin: clamp(10px, 2vw, 24px) 0 clamp(28px, 5vw, 56px);
    text-transform: none;
    max-width: 18ch;
  }
  @media (min-width: 900px) {
    .sw-hero__title {
      /* hero occupies a disciplined column span, not the full bleed */
      max-width: none;
      width: 75%;
    }
  }
  .sw-hero__line {
    display: block;
    position: relative;
  }
  .sw-hero__red {
    color: var(--blue);
  }
  .sw-hero__accent {
    display: inline-block;
    width: clamp(28px, 6vw, 90px);
    height: clamp(14px, 2.4vw, 34px);
    background: var(--red);
    margin-left: clamp(10px, 2vw, 28px);
    vertical-align: middle;
  }
  .sw-hero__foot {
    display: grid;
    grid-template-columns: 1fr;
    gap: clamp(10px, 2vw, 16px);
    max-width: 62ch;
    margin-left: auto;
  }
  @media (min-width: 720px) {
    .sw-hero__foot {
      grid-template-columns: repeat(12, 1fr);
    }
    .sw-hero__tagline {
      grid-column: 7 / 13;
    }
  }
  .sw-hero__tagline {
    font-size: var(--step-1);
    line-height: 1.5;
    max-width: 44ch;
  }
  .sw-hero__tagline--en {
    color: rgba(24, 20, 13, 0.55);
    font-size: var(--step-0);
  }
  .sw-hero__index {
    display: flex;
    flex-wrap: wrap;
    gap: clamp(14px, 4vw, 48px);
    font-size: 0.7rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(24, 20, 13, 0.55);
  }

  /* ── marquee ── */
  .sw-marquee {
    position: relative;
    z-index: 1;
    border-top: 3px solid var(--ink);
    border-bottom: 1px solid var(--hair);
    overflow: hidden;
    background: var(--paper);
  }
  .sw-marquee__track {
    display: inline-flex;
    white-space: nowrap;
    will-change: transform;
    animation: sw-scroll 34s linear infinite;
  }
  .sw-marquee:hover .sw-marquee__track {
    animation-play-state: paused;
  }
  .sw-marquee__group {
    display: inline-flex;
    align-items: center;
    gap: clamp(16px, 3vw, 40px);
    padding: 14px clamp(16px, 3vw, 40px) 14px 0;
    font-family: var(--font-grotesk);
    font-weight: 600;
    font-size: var(--step-2);
    letter-spacing: -0.01em;
    text-transform: uppercase;
  }
  .sw-marquee__sep {
    color: var(--red);
  }
  @keyframes sw-scroll {
    to {
      transform: translateX(-50%);
    }
  }

  /* ── sections ── */
  .sw-section {
    position: relative;
    z-index: 1;
    /* transparent so the faint scroll-driven grid reads through the paper */
    background: transparent;
    padding-block: clamp(56px, 9vw, 128px);
  }
  .sw-section--dark {
    background: var(--ink);
    color: var(--paper);
  }
  .sw-section--muted {
    background: var(--paper-2);
  }
  .sw-section__head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: var(--gutter);
  }
  .sw-section__title {
    font-family: var(--font-grotesk);
    font-weight: 700;
    /* prominent within the scale, but deliberately below the hero step */
    font-size: var(--step-4);
    letter-spacing: -0.025em;
    line-height: 1;
  }
  .sw-section__title--sm {
    font-size: var(--step-3);
  }

  /* ── about ── */
  .sw-about {
    display: grid;
    grid-template-columns: 1fr;
    gap: clamp(28px, 5vw, 56px);
  }
  @media (min-width: 900px) {
    .sw-about {
      grid-template-columns: repeat(12, 1fr);
      gap: var(--gutter);
    }
    .sw-about__lead {
      grid-column: 1 / 6;
    }
    .sw-about__list {
      grid-column: 7 / 13;
    }
  }
  .sw-about__lead {
    font-family: var(--font-grotesk);
    font-weight: 500;
    font-size: var(--step-2);
    line-height: 1.28;
    letter-spacing: -0.02em;
  }
  .sw-about__list {
    list-style: none;
    margin: 0;
    padding: 0;
  }
  .sw-about__item {
    display: grid;
    grid-template-columns: 3.2em 1fr;
    gap: 16px;
    align-items: baseline;
    padding: clamp(14px, 2vw, 20px) 0;
    border-top: 1px solid var(--hair);
  }
  .sw-about__item:last-child {
    border-bottom: 1px solid var(--hair);
  }
  .sw-about__num {
    font-size: 0.78rem;
    color: var(--red);
  }
  .sw-about__text {
    font-size: var(--step-0);
    line-height: 1.55;
  }

  /* ── stack index/table ── */
  .sw-index {
    width: 100%;
  }
  .sw-index__header {
    display: none;
    grid-template-columns: 3.5em 9em 1fr;
    gap: var(--gutter);
    font-size: 0.68rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: color-mix(in srgb, var(--paper) 50%, transparent);
    padding-bottom: 14px;
  }
  @media (min-width: 760px) {
    .sw-index__header {
      display: grid;
    }
  }
  .sw-index__row {
    display: grid;
    grid-template-columns: 1fr;
    gap: 8px var(--gutter);
    padding: clamp(18px, 2.6vw, 28px) 0;
    border-top: 1px solid var(--hair-inv);
    transition: background 0.3s ease;
  }
  .sw-index__row:last-child {
    border-bottom: 1px solid var(--hair-inv);
  }
  @media (min-width: 760px) {
    .sw-index__row {
      grid-template-columns: 3.5em 9em 1fr;
      align-items: baseline;
    }
  }
  .sw-index__row:hover {
    background: color-mix(in srgb, var(--paper) 5%, transparent);
  }
  /* category numerals on the dark panel — 청, lifted toward hanji for contrast */
  .sw-index__num {
    font-size: 0.82rem;
    color: color-mix(in srgb, var(--red) 55%, var(--paper));
  }
  .sw-index__cat {
    font-family: var(--font-grotesk);
    font-weight: 700;
    font-size: var(--step-1);
    letter-spacing: -0.01em;
    line-height: 1.05;
  }
  .sw-index__items {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 6px 0;
    align-items: baseline;
  }
  .sw-index__item {
    font-size: var(--step-0);
    line-height: 1.4;
    padding-right: 14px;
    margin-right: 14px;
    position: relative;
    color: color-mix(in srgb, var(--paper) 82%, transparent);
    transition: color 0.2s ease;
  }
  .sw-index__item::after {
    content: '';
    position: absolute;
    right: 0;
    top: 0.18em;
    bottom: 0.18em;
    width: 1px;
    background: color-mix(in srgb, var(--paper) 22%, transparent);
  }
  .sw-index__item:last-child {
    margin-right: 0;
    padding-right: 0;
  }
  .sw-index__item:last-child::after {
    display: none;
  }
  .sw-index__item:hover {
    color: color-mix(in srgb, var(--blue) 70%, var(--paper));
  }

  /* ── learning ── */
  .sw-learning__note {
    font-size: 0.72rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgba(24, 20, 13, 0.5);
    margin-bottom: clamp(18px, 3vw, 30px);
  }
  .sw-learning {
    display: grid;
    grid-template-columns: 1fr;
  }
  .sw-learning__row {
    display: grid;
    grid-template-columns: 1fr;
    gap: 6px var(--gutter);
    padding: clamp(14px, 2vw, 20px) 0;
    border-top: 1px solid var(--hair);
  }
  .sw-learning__row:last-child {
    border-bottom: 1px solid var(--hair);
  }
  @media (min-width: 700px) {
    .sw-learning__row {
      grid-template-columns: 11em 1fr;
      align-items: baseline;
    }
  }
  .sw-learning__cat {
    font-family: var(--font-grotesk);
    font-weight: 600;
    font-size: var(--step-0);
    color: rgba(24, 20, 13, 0.72);
    letter-spacing: -0.005em;
  }
  .sw-learning__items {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 4px 0;
  }
  .sw-learning__item {
    font-size: var(--step--1);
    color: rgba(24, 20, 13, 0.62);
    padding-right: 12px;
    margin-right: 12px;
    position: relative;
  }
  .sw-learning__item::after {
    content: '·';
    position: absolute;
    right: 2px;
    color: rgba(24, 20, 13, 0.3);
  }
  .sw-learning__item:last-child {
    margin-right: 0;
    padding-right: 0;
  }
  .sw-learning__item:last-child::after {
    display: none;
  }

  /* ── contact ── */
  .sw-contact {
    display: grid;
    grid-template-columns: 1fr;
    gap: clamp(36px, 6vw, 64px);
  }
  @media (min-width: 900px) {
    .sw-contact {
      grid-template-columns: repeat(12, 1fr);
      gap: var(--gutter);
    }
    .sw-contact__main {
      grid-column: 1 / 9;
    }
    .sw-contact__links {
      grid-column: 10 / 13;
    }
  }
  .sw-contact__kicker {
    font-size: 0.72rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--red);
    margin-bottom: clamp(12px, 2vw, 18px);
  }
  .sw-contact__email {
    font-family: var(--font-grotesk);
    font-weight: 700;
    font-size: var(--step-3);
    letter-spacing: -0.025em;
    line-height: 1.02;
    word-break: break-word;
    margin-bottom: clamp(22px, 4vw, 36px);
  }
  .sw-contact__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
  }
  .sw-btn {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    background: transparent;
    color: var(--ink);
    border: 2px solid var(--ink);
    padding: 12px 18px;
    font-family: var(--font-mono);
    font-size: 0.8rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    text-decoration: none;
    cursor: pointer;
    transition: background 0.2s ease, color 0.2s ease;
  }
  .sw-btn:hover,
  .sw-btn:focus-visible {
    background: var(--ink);
    color: var(--paper);
  }
  .sw-btn--red {
    border-color: var(--blue);
    color: var(--blue);
  }
  .sw-btn--red:hover,
  .sw-btn--red:focus-visible {
    background: var(--blue);
    color: var(--paper);
  }
  .sw-btn__mark {
    font-size: 0.85em;
  }
  .sw-contact__feedback {
    min-height: 1.4em;
    margin-top: 14px;
    font-size: 0.78rem;
    letter-spacing: 0.06em;
    color: var(--blue);
    opacity: 0;
    transform: translateY(4px);
    transition: opacity 0.25s ease, transform 0.25s ease;
  }
  .sw-contact__feedback.is-on {
    opacity: 1;
    transform: none;
  }
  .sw-contact__links {
    display: flex;
    flex-direction: column;
    align-self: start;
  }
  .sw-link {
    display: grid;
    grid-template-columns: 2.4em 1fr auto;
    align-items: baseline;
    gap: 10px;
    padding: clamp(14px, 2vw, 18px) 0;
    border-top: 1px solid var(--hair);
    text-decoration: none;
    color: var(--ink);
    transition: padding-left 0.25s cubic-bezier(0.16, 1, 0.3, 1);
  }
  .sw-link:last-child {
    border-bottom: 1px solid var(--hair);
  }
  /* 태극 interplay: blue link indices shift to red on hover */
  .sw-link:hover,
  .sw-link:focus-visible {
    padding-left: 8px;
    color: var(--blue);
  }
  .sw-link__idx {
    font-size: 0.72rem;
    color: var(--red);
  }
  .sw-link:hover .sw-link__idx,
  .sw-link:focus-visible .sw-link__idx {
    color: var(--blue);
  }
  .sw-link__name {
    font-family: var(--font-grotesk);
    font-weight: 600;
    font-size: var(--step-1);
    letter-spacing: -0.01em;
  }
  .sw-link__arrow {
    font-size: 1rem;
  }

  /* ── footer ── */
  .sw-footer {
    position: relative;
    z-index: 1;
    background: transparent;
    padding-bottom: clamp(28px, 4vw, 48px);
  }
  .sw-footer__row {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    gap: 12px;
    padding-top: 18px;
  }
  /* 좌·우 1fr 동일 폭 + 가운데 확실한 중앙 정렬 (콘텐츠 폭에 영향받지 않음) */
  .sw-footer__copy {
    grid-column: 1;
    justify-self: start;
    text-align: left;
  }
  .sw-footer__sys {
    grid-column: 2;
    justify-self: center;
    text-align: center;
  }
  .sw-footer__top {
    grid-column: 3;
    justify-self: end;
    text-align: right;
  }
  .sw-footer__copy,
  .sw-footer__sys,
  .sw-footer__top {
    font-size: 0.7rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(24, 20, 13, 0.6);
  }
  .sw-footer__sys {
    display: none;
  }
  @media (min-width: 760px) {
    .sw-footer__sys {
      display: inline;
    }
  }
  .sw-footer__top {
    text-decoration: none;
    color: var(--ink);
    transition: color 0.2s ease;
  }
  .sw-footer__top:hover {
    color: var(--blue);
  }

  /* ── focus-visible (global, system-respecting) ── */
  .sw a:focus-visible,
  .sw button:focus-visible {
    outline: 2px solid var(--red);
    outline-offset: 3px;
  }

  /* ── staggered reveal for gridded rows ── */
  .sw .sw-about__item.reveal,
  .sw .sw-index__row.reveal,
  .sw .sw-learning__row.reveal {
    transition-delay: calc(var(--i, 0) * 60ms);
  }

  /* ── reduced motion ── */
  @media (prefers-reduced-motion: reduce) {
    .sw-marquee__track {
      animation: none;
    }
    .sw-brand__mark {
      transition: none;
    }
    .sw .reveal {
      transition: none !important;
    }
    /* background stays static — no parallax */
    .sw-bg__cols,
    .sw-bg__baselines {
      transform: none !important;
      will-change: auto;
    }
  }
</style>

<script>
  (() => {
    const root = document.querySelector<HTMLElement>('.sw');
    if (!root) return;

    /* ── visible grid overlay: toggle with the "G" key (deliberate design tool) ── */
    const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    document.addEventListener('keydown', (e) => {
      if (e.key === 'g' || e.key === 'G') {
        const tag = (e.target as HTMLElement)?.tagName;
        if (tag === 'INPUT' || tag === 'TEXTAREA') return;
        root.dataset.grid = root.dataset.grid === 'on' ? 'off' : 'on';
      }
    });

    /* ── email: copy to clipboard with visible feedback ── */
    const email = 'sihoon.cho.business@gmail.com';
    const copyBtn = document.querySelector<HTMLButtonElement>('[data-copy]');
    const feedback = document.querySelector<HTMLElement>('[data-feedback]');
    let timer: number | undefined;

    const flash = (msg: string) => {
      if (!feedback) return;
      feedback.textContent = msg;
      feedback.classList.add('is-on');
      if (timer) window.clearTimeout(timer);
      timer = window.setTimeout(() => {
        feedback.classList.remove('is-on');
      }, 2000);
    };

    const legacyCopy = (text: string) => {
      const ta = document.createElement('textarea');
      ta.value = text;
      ta.setAttribute('readonly', '');
      ta.style.position = 'absolute';
      ta.style.left = '-9999px';
      document.body.appendChild(ta);
      ta.select();
      let ok = false;
      try {
        ok = document.execCommand('copy');
      } catch {
        ok = false;
      }
      document.body.removeChild(ta);
      return ok;
    };

    copyBtn?.addEventListener('click', async () => {
      try {
        if (navigator.clipboard && window.isSecureContext) {
          await navigator.clipboard.writeText(email);
          flash('복사되었습니다!');
        } else {
          flash(legacyCopy(email) ? '복사되었습니다!' : '복사 실패 — ' + email);
        }
      } catch {
        flash(legacyCopy(email) ? '복사되었습니다!' : '복사 실패 — ' + email);
      }
    });

    /* ── scroll-driven background: rAF-throttled parallax of the grid system ── */
    const cols = document.querySelector<HTMLElement>('[data-bg-cols]');
    const lines = document.querySelector<HTMLElement>('[data-bg-lines]');
    if (!reduced && (cols || lines)) {
      let ticking = false;
      const render = () => {
        const y = window.scrollY || 0;
        // restrained, systematic offsets — columns drift slowly, baselines a touch more
        if (cols) cols.style.transform = `translate3d(0, ${(y * -0.04).toFixed(2)}px, 0)`;
        if (lines) lines.style.transform = `translate3d(0, ${(y * 0.06).toFixed(2)}px, 0)`;
        ticking = false;
      };
      const onScroll = () => {
        if (ticking) return;
        ticking = true;
        window.requestAnimationFrame(render);
      };
      window.addEventListener('scroll', onScroll, { passive: true });
      render();
    }

    /* ── marquee: seamless duplication already in markup; nothing else needed ── */
  })();
</script>
