---
import Base from '../../layouts/Base.astro';
import { site, about, stack, learning } from '../../data/site';
import { getTech, techLogo } from '../../data/tech';

// 브랜드 hex 의 상대 휘도를 계산해 "어두운 색"인지 판별 (다크 배경 대비 처리용)
function isDarkColor(hex: string): boolean {
  const h = hex.replace('#', '');
  if (h.length !== 6) return false;
  const r = parseInt(h.slice(0, 2), 16) / 255;
  const g = parseInt(h.slice(2, 4), 16) / 255;
  const b = parseInt(h.slice(4, 6), 16) / 255;
  const lin = (c: number) => (c <= 0.03928 ? c / 12.92 : Math.pow((c + 0.055) / 1.055, 2.4));
  const lum = 0.2126 * lin(r) + 0.7152 * lin(g) + 0.0722 * lin(b);
  return lum < 0.16;
}

const year = 2026;
const navLinks = [
  { href: '#about', label: 'About' },
  { href: '#stack', label: 'Stack' },
  { href: '#learning', label: 'Learning' },
  { href: '#contact', label: 'Contact' },
];
const stats = [
  { value: 8, label: 'Stack domains', suffix: '' },
  { value: 30, label: 'Tools & frameworks', suffix: '+' },
  { value: 100, label: 'Ship to prod', suffix: '%' },
];
---

<Base title="Sihoon Cho — Kinetic Minimal">
  <div class="kinetic" data-kinetic>
    <!-- scroll progress -->
    <div class="kinetic-progress" aria-hidden="true"><span data-progress></span></div>

    <!-- scroll-driven ambient background (faint grid + parallax glow) -->
    <div class="kinetic-bg" data-bg aria-hidden="true">
      <span class="kinetic-bg__grid" data-bg-grid></span>
      <span class="kinetic-bg__glow kinetic-bg__glow--a" data-bg-glow-a></span>
      <span class="kinetic-bg__glow kinetic-bg__glow--b" data-bg-glow-b></span>
    </div>

    <!-- ambient hero glow (single, very subtle) -->
    <div class="kinetic-ambient" aria-hidden="true"></div>

    <!-- ───────── header ───────── -->
    <header class="kinetic-header" data-header role="banner">
      <div class="kinetic-header__inner">
        <a class="kinetic-brand" href="#top" aria-label="Sihoon Cho — top">
          <span class="kinetic-brand__mark" aria-hidden="true"></span>
          <span class="kinetic-brand__name">{site.name}</span>
          <span class="kinetic-brand__ko" aria-hidden="true">{site.nameKo}</span>
        </a>
        <nav class="kinetic-nav" aria-label="Primary">
          {navLinks.map((l) => <a href={l.href}>{l.label}</a>)}
        </nav>
        <a class="kinetic-btn kinetic-btn--ghost" data-magnetic href="#contact">
          <span data-magnetic-inner>Get in touch</span>
        </a>
      </div>
    </header>

    <main id="top">
      <!-- ───────── hero ───────── -->
      <section class="kinetic-hero" aria-labelledby="hero-title">
        <div class="kinetic-shell">
          <p class="kinetic-eyebrow reveal">
            <span class="kinetic-dot" aria-hidden="true"></span>
            <span class="kinetic-mono">{site.role}</span>
          </p>
          <h1 id="hero-title" class="kinetic-hero__title reveal">
            <span class="kinetic-line">{site.name}</span>
            <span class="kinetic-line kinetic-grad">Reliable systems,</span>
            <span class="kinetic-line">shipped &amp; operated.</span>
          </h1>
          <p class="kinetic-hero__sub reveal">{site.tagline}</p>
          <p class="kinetic-hero__sub kinetic-hero__sub--en reveal">{site.taglineEn}</p>
          <div class="kinetic-hero__cta reveal">
            <a class="kinetic-btn kinetic-btn--solid" data-magnetic href="#contact">
              <span data-magnetic-inner>Contact me</span>
              <svg viewBox="0 0 16 16" width="14" height="14" aria-hidden="true" class="kinetic-arrow"
                ><path
                  d="M3 8h9M8.5 4l4 4-4 4"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="1.5"
                  stroke-linecap="round"
                  stroke-linejoin="round"></path></svg
              >
            </a>
            <a
              class="kinetic-btn kinetic-btn--ghost"
              data-magnetic
              href={site.links.github}
              target="_blank"
              rel="noopener noreferrer"
            >
              <span data-magnetic-inner>GitHub ↗</span>
            </a>
          </div>
        </div>
        <span class="kinetic-scrollcue" aria-hidden="true">
          <span class="kinetic-scrollcue__line"></span>
          <span class="kinetic-mono">scroll</span>
        </span>
      </section>

      <!-- ───────── about ───────── -->
      <section class="kinetic-section" id="about" aria-labelledby="about-title">
        <div class="kinetic-shell">
          <header class="kinetic-sechead reveal">
            <span class="kinetic-label kinetic-mono">About</span>
            <h2 id="about-title" class="kinetic-sectitle">{about.lead}</h2>
          </header>

          <ul class="kinetic-stats reveal" aria-label="Highlights">
            {
              stats.map((s) => (
                <li class="kinetic-stat" data-card>
                  <span class="kinetic-card-glow" aria-hidden="true" />
                  <span class="kinetic-stat__num">
                    <span data-count={s.value}>0</span>
                    {s.suffix}
                  </span>
                  <span class="kinetic-stat__label kinetic-mono">{s.label}</span>
                </li>
              ))
            }
          </ul>

          <ul class="kinetic-highlights">
            {
              about.highlights.map((h, i) => (
                <li class="kinetic-hl reveal" data-card style={`transition-delay:${i * 70}ms`}>
                  <span class="kinetic-card-glow" aria-hidden="true" />
                  <span class="kinetic-hl__idx kinetic-mono" aria-hidden="true">
                    {String(i + 1).padStart(2, '0')}
                  </span>
                  <span class="kinetic-hl__text">{h}</span>
                </li>
              ))
            }
          </ul>
        </div>
      </section>

      <!-- ───────── tech stack ───────── -->
      <section class="kinetic-section" id="stack" aria-labelledby="stack-title">
        <div class="kinetic-shell">
          <header class="kinetic-sechead reveal">
            <span class="kinetic-label kinetic-mono">Tech Stack</span>
            <h2 id="stack-title" class="kinetic-sectitle kinetic-sectitle--sm">
              주력으로 다루는 기술
            </h2>
          </header>

          <div class="kinetic-rows">
            {
              stack.map((group, i) => (
                <article class="kinetic-row reveal" data-card style={`transition-delay:${i * 50}ms`}>
                  <span class="kinetic-card-glow" aria-hidden="true" />
                  <div class="kinetic-row__head">
                    <h3 class="kinetic-row__cat">{group.category}</h3>
                    <span class="kinetic-row__count kinetic-mono" aria-hidden="true">
                      {String(group.items.length).padStart(2, '0')}
                    </span>
                  </div>
                  <ul class="kinetic-chips" aria-label={group.category}>
                    {group.items.map((name) => {
                      const { color } = getTech(name);
                      const dark = isDarkColor(color);
                      const logo = techLogo(name, dark ? '#e7e7ea' : color);
                      return (
                        <li
                          class="kinetic-chip"
                          style={`--bc:${color};--bc-tint:${color}1f`}
                        >
                          {logo && (
                            <span class:list={['kinetic-chip__tile', { 'is-dark': dark }]}>
                              <img
                                src={logo}
                                alt=""
                                width="16"
                                height="16"
                                loading="lazy"
                                data-techlogo
                              />
                            </span>
                          )}
                          <span class="kinetic-chip__name">{name}</span>
                        </li>
                      );
                    })}
                  </ul>
                </article>
              ))
            }
          </div>
        </div>
      </section>

      <!-- ───────── learning ───────── -->
      <section class="kinetic-section kinetic-section--muted" id="learning" aria-labelledby="learning-title">
        <div class="kinetic-shell">
          <header class="kinetic-sechead reveal">
            <span class="kinetic-label kinetic-mono">Learning</span>
            <h2 id="learning-title" class="kinetic-sectitle kinetic-sectitle--sm">
              탐색하고 익히는 중
            </h2>
          </header>

          <div class="kinetic-learn">
            {
              learning.map((group, i) => (
                <article class="kinetic-learn__col reveal" style={`transition-delay:${i * 50}ms`}>
                  <h3 class="kinetic-learn__cat kinetic-mono">{group.category}</h3>
                  <ul class="kinetic-learn__items">
                    {group.items.map((name) => {
                      const { color } = getTech(name);
                      const dark = isDarkColor(color);
                      const logo = techLogo(name, dark ? '#e7e7ea' : color);
                      return (
                        <li
                          class="kinetic-chip kinetic-chip--learn"
                          style={`--bc:${color};--bc-tint:${color}1f`}
                        >
                          {logo && (
                            <span class:list={['kinetic-chip__tile', { 'is-dark': dark }]}>
                              <img
                                src={logo}
                                alt=""
                                width="16"
                                height="16"
                                loading="lazy"
                                data-techlogo
                              />
                            </span>
                          )}
                          <span class="kinetic-chip__name">{name}</span>
                        </li>
                      );
                    })}
                  </ul>
                </article>
              ))
            }
          </div>
        </div>
      </section>

      <!-- ───────── contact ───────── -->
      <section class="kinetic-section" id="contact" aria-labelledby="contact-title">
        <div class="kinetic-shell">
          <div class="kinetic-contact reveal" data-card>
            <span class="kinetic-card-glow" aria-hidden="true"></span>
            <header class="kinetic-sechead">
              <span class="kinetic-label kinetic-mono">Contact</span>
              <h2 id="contact-title" class="kinetic-sectitle kinetic-sectitle--sm">
                같이 만들 거리가 있다면, 편하게 연락 주세요.
              </h2>
            </header>

            <div class="kinetic-mailrow">
              <a
                class="kinetic-btn kinetic-btn--solid"
                data-magnetic
                href="https://mail.google.com/mail/?view=cm&fs=1&to=sihoon.cho.business@gmail.com"
                target="_blank"
                rel="noopener noreferrer"
              >
                <span data-magnetic-inner>Gmail로 보내기 ↗</span>
              </a>
              <button
                type="button"
                class="kinetic-mail"
                data-copy
                aria-label={`이메일 주소 복사: ${site.email}`}
              >
                <span class="kinetic-mail__addr kinetic-mono">{site.email}</span>
                <span class="kinetic-mail__hint kinetic-mono" data-copy-label>복사</span>
              </button>
            </div>
            <p class="kinetic-copyfeedback kinetic-mono" data-copy-feedback role="status" aria-live="polite"></p>

            <ul class="kinetic-social">
              <li>
                <a href={site.links.github} target="_blank" rel="noopener noreferrer">GitHub ↗</a>
              </li>
              <li>
                <a href={site.links.linkedin} target="_blank" rel="noopener noreferrer">
                  LinkedIn ↗
                </a>
              </li>
              <li>
                <a href={site.links.velog} target="_blank" rel="noopener noreferrer">Velog ↗</a>
              </li>
            </ul>
          </div>
        </div>
      </section>
    </main>

    <footer class="kinetic-footer">
      <div class="kinetic-shell kinetic-footer__inner">
        <span class="kinetic-mono">© {year} {site.name} · {site.nameKo}</span>
        <span class="kinetic-mono kinetic-footer__dim">{site.role}</span>
      </div>
    </footer>
  </div>
</Base>

<style>
  .kinetic {
    --bg: #08080a;
    --surface-1: #111114;
    --surface-2: #16161a;
    --line: rgba(255, 255, 255, 0.08);
    --line-strong: rgba(255, 255, 255, 0.14);
    --text: #f4f4f5;
    --muted: #a1a1aa;
    --faint: #6b6b73;
    --accent: #7c8cff;
    --accent-soft: rgba(124, 140, 255, 0.16);
    --spring: cubic-bezier(0.2, 0.9, 0.2, 1.05);
    --ease: cubic-bezier(0.16, 1, 0.3, 1);

    position: relative;
    background: var(--bg);
    color: var(--text);
    min-height: 100vh;
    font-family: var(--font-sans);
    overflow-x: clip;
  }

  /* ── scroll progress ── */
  .kinetic-progress {
    position: fixed;
    inset: 0 0 auto 0;
    height: 2px;
    z-index: 60;
    background: transparent;
    pointer-events: none;
  }
  .kinetic-progress span {
    display: block;
    height: 100%;
    width: 100%;
    transform: scaleX(0);
    transform-origin: 0 50%;
    background: linear-gradient(90deg, transparent, var(--accent));
  }

  /* ── ambient glow ── */
  .kinetic-ambient {
    position: absolute;
    top: -14rem;
    left: 50%;
    width: min(72rem, 120vw);
    height: 44rem;
    transform: translateX(-50%);
    background: radial-gradient(
      55% 50% at 50% 40%,
      rgba(124, 140, 255, 0.1),
      transparent 70%
    );
    filter: blur(20px);
    pointer-events: none;
    z-index: 0;
  }

  /* ── scroll-driven ambient background ── */
  .kinetic-bg {
    position: fixed;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    overflow: hidden;
  }
  .kinetic-bg__grid {
    --gp: 0px;
    position: absolute;
    inset: -20% -20% -20% -20%;
    background-image: linear-gradient(rgba(255, 255, 255, 0.025) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.025) 1px, transparent 1px);
    background-size: 64px 64px;
    background-position: 0 var(--gp);
    transform: translateY(var(--gy, 0));
    -webkit-mask-image: radial-gradient(120% 90% at 50% 8%, #000 35%, transparent 78%);
    mask-image: radial-gradient(120% 90% at 50% 8%, #000 35%, transparent 78%);
    will-change: transform, background-position;
  }
  .kinetic-bg__glow {
    position: absolute;
    border-radius: 50%;
    filter: blur(70px);
    will-change: transform;
  }
  .kinetic-bg__glow--a {
    top: 8%;
    left: 8%;
    width: 34rem;
    height: 34rem;
    background: radial-gradient(circle, rgba(124, 140, 255, 0.09), transparent 68%);
    transform: translate3d(0, var(--ga, 0), 0);
  }
  .kinetic-bg__glow--b {
    bottom: 4%;
    right: 6%;
    width: 30rem;
    height: 30rem;
    background: radial-gradient(circle, rgba(99, 179, 237, 0.06), transparent 68%);
    transform: translate3d(0, var(--gb, 0), 0);
  }

  /* ── shell ── */
  .kinetic-shell {
    width: 100%;
    max-width: 72rem;
    margin-inline: auto;
    padding-inline: clamp(1.25rem, 5vw, 3rem);
    position: relative;
    z-index: 1;
  }
  .kinetic-mono {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  /* ── header ── */
  .kinetic-header {
    position: sticky;
    top: 0;
    z-index: 50;
    border-bottom: 1px solid transparent;
    transition: border-color 0.4s var(--ease), background-color 0.4s var(--ease),
      backdrop-filter 0.4s var(--ease);
  }
  .kinetic-header.is-scrolled {
    background: rgba(8, 8, 10, 0.72);
    backdrop-filter: blur(12px) saturate(140%);
    border-bottom-color: var(--line);
  }
  .kinetic-header__inner {
    max-width: 72rem;
    margin-inline: auto;
    padding: clamp(0.9rem, 2vw, 1.2rem) clamp(1.25rem, 5vw, 3rem);
    display: flex;
    align-items: center;
    gap: 1.5rem;
    transition: padding 0.4s var(--ease);
  }
  .kinetic-header.is-scrolled .kinetic-header__inner {
    padding-top: 0.65rem;
    padding-bottom: 0.65rem;
  }
  .kinetic-brand {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    text-decoration: none;
    color: var(--text);
    font-weight: 600;
    letter-spacing: -0.01em;
  }
  .kinetic-brand__mark {
    width: 16px;
    height: 16px;
    border-radius: 4px;
    background: linear-gradient(140deg, #fff, #8b93ff 130%);
    box-shadow: 0 0 0 1px var(--line-strong), 0 0 14px rgba(124, 140, 255, 0.35);
    transition: transform 0.5s var(--spring);
  }
  .kinetic-brand:hover .kinetic-brand__mark {
    transform: rotate(90deg) scale(1.05);
  }
  .kinetic-brand__ko {
    color: var(--faint);
    font-size: 0.85rem;
    font-weight: 500;
  }
  .kinetic-nav {
    margin-left: auto;
    display: none;
    gap: 0.35rem;
  }
  .kinetic-nav a {
    position: relative;
    padding: 0.45rem 0.7rem;
    font-size: 0.85rem;
    color: var(--muted);
    text-decoration: none;
    border-radius: 6px;
    transition: color 0.25s var(--ease), background-color 0.25s var(--ease);
  }
  .kinetic-nav a:hover {
    color: var(--text);
    background: rgba(255, 255, 255, 0.04);
  }
  @media (min-width: 820px) {
    .kinetic-nav {
      display: inline-flex;
    }
  }

  /* ── buttons ── */
  .kinetic-btn {
    --pad-x: 1rem;
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    padding: 0.6rem var(--pad-x);
    font-size: 0.85rem;
    font-weight: 500;
    border-radius: 7px;
    text-decoration: none;
    cursor: pointer;
    border: 1px solid var(--line);
    color: var(--text);
    background: var(--surface-1);
    will-change: transform;
    transition: transform 0.45s var(--spring), border-color 0.3s var(--ease),
      background-color 0.3s var(--ease), box-shadow 0.3s var(--ease);
  }
  .kinetic-btn:hover {
    border-color: var(--line-strong);
    background: var(--surface-2);
  }
  .kinetic-btn--solid {
    background: #fafafa;
    color: #09090b;
    border-color: transparent;
  }
  .kinetic-btn--solid:hover {
    background: #fff;
    box-shadow: 0 6px 24px -8px rgba(124, 140, 255, 0.5);
  }
  .kinetic-btn--ghost {
    margin-left: auto;
  }
  @media (min-width: 820px) {
    .kinetic-btn--ghost {
      margin-left: 0;
    }
  }
  .kinetic-arrow {
    transition: transform 0.4s var(--spring);
  }
  .kinetic-btn:hover .kinetic-arrow {
    transform: translateX(3px);
  }

  /* ── hero ── */
  .kinetic-hero {
    position: relative;
    padding: clamp(5rem, 16vh, 11rem) 0 clamp(4rem, 10vh, 8rem);
    min-height: 86vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }
  .kinetic-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    color: var(--muted);
    margin-bottom: 1.6rem;
  }
  .kinetic-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 0 4px var(--accent-soft);
    animation: kinetic-pulse 2.4s var(--ease) infinite;
  }
  @keyframes kinetic-pulse {
    0%,
    100% {
      box-shadow: 0 0 0 3px var(--accent-soft);
    }
    50% {
      box-shadow: 0 0 0 7px transparent;
    }
  }
  .kinetic-hero__title {
    font-family: var(--font-grotesk);
    font-weight: 600;
    line-height: 1.02;
    letter-spacing: -0.035em;
    font-size: clamp(2.6rem, 8.5vw, 6rem);
    margin: 0 0 1.8rem;
  }
  .kinetic-line {
    display: block;
  }
  .kinetic-grad {
    background: linear-gradient(100deg, #f4f4f5 10%, #9aa2ff 55%, #f4f4f5 90%);
    background-size: 220% 100%;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    color: transparent;
    animation: kinetic-sheen 7s linear infinite;
  }
  @keyframes kinetic-sheen {
    to {
      background-position: 220% 0;
    }
  }
  .kinetic-hero__sub {
    max-width: 40rem;
    font-size: clamp(1rem, 2.2vw, 1.18rem);
    line-height: 1.6;
    color: var(--muted);
    margin: 0 0 0.4rem;
  }
  .kinetic-hero__sub--en {
    color: var(--faint);
    font-size: 0.95rem;
  }
  .kinetic-hero__cta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.8rem;
    margin-top: 2.4rem;
  }
  .kinetic-scrollcue {
    position: absolute;
    bottom: clamp(1.5rem, 5vh, 3rem);
    left: clamp(1.25rem, 5vw, 3rem);
    display: flex;
    align-items: center;
    gap: 0.7rem;
    color: var(--faint);
  }
  .kinetic-scrollcue__line {
    width: 36px;
    height: 1px;
    background: linear-gradient(90deg, var(--faint), transparent);
    position: relative;
    overflow: hidden;
  }
  .kinetic-scrollcue__line::after {
    content: '';
    position: absolute;
    inset: 0;
    width: 14px;
    background: var(--accent);
    animation: kinetic-slide 2.2s var(--ease) infinite;
  }
  @keyframes kinetic-slide {
    from {
      transform: translateX(-14px);
    }
    to {
      transform: translateX(36px);
    }
  }

  /* ── sections ── */
  .kinetic-section {
    position: relative;
    padding: clamp(4rem, 10vh, 7.5rem) 0;
    border-top: 1px solid var(--line);
  }
  .kinetic-section--muted {
    background: linear-gradient(180deg, rgba(255, 255, 255, 0.012), transparent 30%);
  }
  .kinetic-sechead {
    margin-bottom: 2.6rem;
    max-width: 52rem;
  }
  .kinetic-label {
    display: inline-block;
    color: var(--accent);
    margin-bottom: 1rem;
  }
  .kinetic-sectitle {
    font-family: var(--font-grotesk);
    font-weight: 600;
    letter-spacing: -0.028em;
    line-height: 1.12;
    font-size: clamp(2rem, 5.6vw, 3.4rem);
    margin: 0;
    color: var(--text);
  }
  .kinetic-sectitle--sm {
    font-size: clamp(1.75rem, 4.4vw, 2.7rem);
  }

  /* ── shared card glow (pointer-tracked) ── */
  [data-card] {
    position: relative;
    isolation: isolate;
  }
  .kinetic-card-glow {
    position: absolute;
    inset: 0;
    border-radius: inherit;
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.4s var(--ease);
    background: radial-gradient(
      18rem 18rem at var(--mx, 50%) var(--my, 50%),
      rgba(124, 140, 255, 0.14),
      transparent 60%
    );
    z-index: -1;
  }
  [data-card]:hover .kinetic-card-glow,
  [data-card]:focus-within .kinetic-card-glow {
    opacity: 1;
  }

  /* ── stats ── */
  .kinetic-stats {
    list-style: none;
    margin: 0 0 2.6rem;
    padding: 0;
    display: grid;
    gap: 1px;
    grid-template-columns: repeat(3, 1fr);
    border: 1px solid var(--line);
    border-radius: 8px;
    overflow: hidden;
    background: var(--line);
  }
  .kinetic-stat {
    background: var(--surface-1);
    padding: 1.5rem 1.4rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    transition: background-color 0.3s var(--ease);
  }
  .kinetic-stat:hover {
    background: var(--surface-2);
  }
  .kinetic-stat__num {
    font-family: var(--font-grotesk);
    font-size: clamp(1.8rem, 5vw, 2.8rem);
    font-weight: 600;
    letter-spacing: -0.03em;
    line-height: 1;
  }
  .kinetic-stat__label {
    color: var(--muted);
  }

  /* ── highlights ── */
  .kinetic-highlights {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 0.75rem;
    grid-template-columns: 1fr;
  }
  @media (min-width: 720px) {
    .kinetic-highlights {
      grid-template-columns: 1fr 1fr;
    }
  }
  .kinetic-hl {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
    padding: 1.25rem 1.4rem;
    border: 1px solid var(--line);
    border-radius: 8px;
    background: var(--surface-1);
    transition: border-color 0.3s var(--ease), transform 0.45s var(--spring),
      background-color 0.3s var(--ease);
  }
  .kinetic-hl:hover {
    border-color: var(--line-strong);
    transform: translateY(-2px);
  }
  .kinetic-hl__idx {
    color: var(--accent);
    flex-shrink: 0;
    padding-top: 0.15rem;
  }
  .kinetic-hl__text {
    color: var(--muted);
    line-height: 1.55;
    font-size: 0.95rem;
  }

  /* ── stack rows ── */
  .kinetic-rows {
    display: grid;
    gap: 0.75rem;
  }
  .kinetic-row {
    border: 1px solid var(--line);
    border-radius: 8px;
    background: var(--surface-1);
    padding: 1.35rem 1.5rem;
    display: grid;
    gap: 1rem;
    grid-template-columns: 1fr;
    transition: border-color 0.3s var(--ease), background-color 0.3s var(--ease);
  }
  @media (min-width: 760px) {
    .kinetic-row {
      grid-template-columns: 13rem 1fr;
      align-items: start;
      gap: 1.5rem;
    }
  }
  .kinetic-row:hover {
    border-color: var(--line-strong);
  }
  .kinetic-row__head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 0.75rem;
  }
  .kinetic-row__cat {
    font-family: var(--font-grotesk);
    font-size: 1.05rem;
    font-weight: 500;
    letter-spacing: -0.01em;
    margin: 0;
    color: var(--text);
  }
  .kinetic-row__count {
    color: var(--faint);
  }
  .kinetic-chips {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
  }
  .kinetic-chip {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    font-size: 0.8rem;
    font-family: var(--font-mono);
    letter-spacing: 0.01em;
    color: var(--muted);
    padding: 0.3rem 0.62rem 0.3rem 0.34rem;
    border: 1px solid var(--line);
    border-bottom: 2px solid var(--bc, var(--accent));
    border-radius: 0;
    background: rgba(255, 255, 255, 0.015);
    /* 양각 입체감 — premium과 동일 효과 (다크 팔레트로 조정) */
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.06), 0 1px 2px rgba(0, 0, 0, 0.4);
    transition: color 0.22s var(--ease), border-color 0.22s var(--ease),
      background-color 0.22s var(--ease), box-shadow 0.22s var(--ease),
      transform 0.4s var(--spring);
  }
  .kinetic-chip:has(.kinetic-chip__tile) {
    padding-left: 0.32rem;
  }
  .kinetic-chip:not(:has(.kinetic-chip__tile)) {
    padding-left: 0.62rem;
  }
  .kinetic-chip:hover {
    color: var(--text);
    border-color: color-mix(in srgb, var(--bc, var(--accent)) 60%, var(--line-strong));
    border-bottom-color: var(--bc, var(--accent));
    background: var(--bc-tint, var(--accent-soft));
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.06),
      0 6px 22px -12px var(--bc, var(--accent));
    transform: translateY(-2px);
  }
  .kinetic-chip__tile {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 22px;
    height: 22px;
    border-radius: 0;
    flex-shrink: 0;
    background: #f4f4f5;
    box-shadow: inset 0 0 0 1px rgba(0, 0, 0, 0.08);
  }
  .kinetic-chip__tile.is-dark {
    background: #18181b;
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.12);
  }
  .kinetic-chip__tile img {
    display: block;
    width: 16px;
    height: 16px;
  }
  .kinetic-chip__name {
    line-height: 1;
  }
  .kinetic-chip--learn {
    font-size: 0.78rem;
  }

  /* ── learning ── */
  .kinetic-learn {
    display: grid;
    gap: 1px;
    grid-template-columns: repeat(2, 1fr);
    border: 1px solid var(--line);
    border-radius: 8px;
    overflow: hidden;
    background: var(--line);
  }
  @media (min-width: 760px) {
    .kinetic-learn {
      grid-template-columns: repeat(3, 1fr);
    }
  }
  .kinetic-learn__col {
    background: var(--bg);
    padding: 1.4rem;
    transition: background-color 0.3s var(--ease);
  }
  .kinetic-learn__col:hover {
    background: var(--surface-1);
  }
  .kinetic-learn__cat {
    color: var(--faint);
    margin: 0 0 0.9rem;
  }
  .kinetic-learn__items {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
  }

  /* ── contact ── */
  .kinetic-contact {
    border: 1px solid var(--line);
    border-radius: 10px;
    background: var(--surface-1);
    padding: clamp(1.6rem, 4vw, 2.8rem);
  }
  .kinetic-mailrow {
    display: flex;
    flex-wrap: wrap;
    gap: 0.8rem;
    margin-top: 0.5rem;
  }
  .kinetic-mail {
    display: inline-flex;
    align-items: center;
    gap: 0.9rem;
    padding: 0.7rem 1rem;
    border: 1px solid var(--line);
    border-radius: 7px;
    background: var(--bg);
    color: var(--text);
    cursor: pointer;
    transition: border-color 0.3s var(--ease), background-color 0.3s var(--ease);
  }
  .kinetic-mail:hover {
    border-color: var(--accent);
    background: var(--surface-2);
  }
  .kinetic-mail__addr {
    font-size: 0.85rem;
    text-transform: none;
    letter-spacing: 0;
  }
  .kinetic-mail__hint {
    color: var(--faint);
    border-left: 1px solid var(--line);
    padding-left: 0.9rem;
  }
  .kinetic-copyfeedback {
    min-height: 1.1rem;
    margin: 0.9rem 0 0;
    color: var(--accent);
    opacity: 0;
    transform: translateY(4px);
    transition: opacity 0.3s var(--ease), transform 0.3s var(--ease);
  }
  .kinetic-copyfeedback.is-on {
    opacity: 1;
    transform: none;
  }
  .kinetic-social {
    list-style: none;
    margin: 1.8rem 0 0;
    padding: 1.6rem 0 0;
    border-top: 1px solid var(--line);
    display: flex;
    flex-wrap: wrap;
    gap: 1.6rem;
  }
  .kinetic-social a {
    color: var(--muted);
    text-decoration: none;
    font-size: 0.9rem;
    position: relative;
    transition: color 0.25s var(--ease);
  }
  .kinetic-social a::after {
    content: '';
    position: absolute;
    left: 0;
    bottom: -3px;
    width: 100%;
    height: 1px;
    background: var(--accent);
    transform: scaleX(0);
    transform-origin: 0 50%;
    transition: transform 0.35s var(--spring);
  }
  .kinetic-social a:hover {
    color: var(--text);
  }
  .kinetic-social a:hover::after {
    transform: scaleX(1);
  }

  /* ── footer ── */
  .kinetic-footer {
    border-top: 1px solid var(--line);
    padding: 2.2rem 0;
  }
  .kinetic-footer__inner {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 0.8rem;
    color: var(--faint);
  }
  .kinetic-footer__dim {
    color: var(--faint);
  }

  /* ── focus visibility ── */
  .kinetic a:focus-visible,
  .kinetic button:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 3px;
    border-radius: 6px;
  }

  @media (prefers-reduced-motion: reduce) {
    .kinetic-dot,
    .kinetic-grad,
    .kinetic-scrollcue__line::after {
      animation: none !important;
    }
    .kinetic-grad {
      -webkit-text-fill-color: var(--text);
      color: var(--text);
    }
    .kinetic-bg__grid,
    .kinetic-bg__glow {
      transform: none !important;
      background-position: 0 0 !important;
    }
  }
</style>

<script>
  (() => {
    const root = document.querySelector<HTMLElement>('[data-kinetic]');
    if (!root) return;
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    // ── scroll-driven UI (one rAF-gated listener: header + progress + parallax) ──
    const header = root.querySelector<HTMLElement>('[data-header]');
    const bar = root.querySelector<HTMLElement>('[data-progress]');
    const grid = root.querySelector<HTMLElement>('[data-bg-grid]');
    const glowA = root.querySelector<HTMLElement>('[data-bg-glow-a]');
    const glowB = root.querySelector<HTMLElement>('[data-bg-glow-b]');
    const animateScroll = !prefersReducedMotion && !!(bar || grid || glowA || glowB);
    if (header || animateScroll) {
      // cached scroll range — recomputed on resize, not on every scroll frame
      let maxScroll = 0;
      const measureMaxScroll = () => {
        maxScroll = document.documentElement.scrollHeight - window.innerHeight;
      };
      let ticking = false;
      const update = () => {
        ticking = false;
        const y = window.scrollY;
        if (header) header.classList.toggle('is-scrolled', y > 12);
        if (animateScroll) {
          if (bar) {
            const p = maxScroll > 0 ? Math.min(y / maxScroll, 1) : 0;
            bar.style.transform = `scaleX(${p})`;
          }
          if (grid) {
            grid.style.setProperty('--gp', `${(y * 0.15) % 64}px`);
            grid.style.setProperty('--gy', `${y * -0.03}px`);
          }
          if (glowA) glowA.style.setProperty('--ga', `${y * 0.12}px`);
          if (glowB) glowB.style.setProperty('--gb', `${y * -0.1}px`);
        }
      };
      const requestUpdate = () => {
        if (!ticking) {
          ticking = true;
          window.requestAnimationFrame(update);
        }
      };
      window.addEventListener('scroll', requestUpdate, { passive: true });
      window.addEventListener(
        'resize',
        () => {
          measureMaxScroll();
          requestUpdate();
        },
        { passive: true }
      );
      measureMaxScroll();
      update();
    }

    // ── graceful missing-logo fallback ──
    document
      .querySelectorAll('img[data-techlogo]')
      .forEach((i) => i.addEventListener('error', () => i.remove()));

    // ── magnetic buttons ──
    if (!prefersReducedMotion) {
      const mags = root.querySelectorAll<HTMLElement>('[data-magnetic]');
      mags.forEach((el) => {
        const inner = el.querySelector<HTMLElement>('[data-magnetic-inner]');
        const strength = 0.28;
        // rAF-gate pointer tracking: one layout read + style write per frame
        let frame = 0;
        let pointerX = 0;
        let pointerY = 0;
        const apply = () => {
          frame = 0;
          const r = el.getBoundingClientRect();
          const x = pointerX - r.left - r.width / 2;
          const y = pointerY - r.top - r.height / 2;
          el.style.transform = `translate(${x * strength}px, ${y * strength}px)`;
          if (inner) inner.style.transform = `translate(${x * strength * 0.4}px, ${y * strength * 0.4}px)`;
        };
        el.addEventListener('pointermove', (e) => {
          pointerX = e.clientX;
          pointerY = e.clientY;
          if (!frame) frame = requestAnimationFrame(apply);
        });
        const reset = () => {
          if (frame) {
            cancelAnimationFrame(frame);
            frame = 0;
          }
          el.style.transform = '';
          if (inner) inner.style.transform = '';
        };
        el.addEventListener('pointerleave', reset);
        el.addEventListener('blur', reset);
      });
    }

    // ── pointer-tracked card glow ──
    if (!prefersReducedMotion) {
      const cards = root.querySelectorAll<HTMLElement>('[data-card]');
      cards.forEach((card) => {
        // rAF-gate: one getBoundingClientRect + style write per frame, not per event
        let frame = 0;
        let pointerX = 0;
        let pointerY = 0;
        const apply = () => {
          frame = 0;
          const r = card.getBoundingClientRect();
          card.style.setProperty('--mx', `${pointerX - r.left}px`);
          card.style.setProperty('--my', `${pointerY - r.top}px`);
        };
        card.addEventListener('pointermove', (e) => {
          pointerX = e.clientX;
          pointerY = e.clientY;
          if (!frame) frame = requestAnimationFrame(apply);
        });
      });
    }

    // ── count-up stats ──
    const counters = root.querySelectorAll<HTMLElement>('[data-count]');
    if (counters.length) {
      const run = (el: HTMLElement) => {
        const target = Number(el.getAttribute('data-count')) || 0;
        if (prefersReducedMotion) {
          el.textContent = String(target);
          return;
        }
        const dur = 1100;
        const start = performance.now();
        const tick = (now: number) => {
          const t = Math.min((now - start) / dur, 1);
          const eased = 1 - Math.pow(1 - t, 3);
          el.textContent = String(Math.round(target * eased));
          if (t < 1) requestAnimationFrame(tick);
        };
        requestAnimationFrame(tick);
      };
      const io = new IntersectionObserver(
        (entries) => {
          for (const e of entries) {
            if (e.isIntersecting) {
              run(e.target as HTMLElement);
              io.unobserve(e.target);
            }
          }
        },
        { threshold: 0.6 }
      );
      counters.forEach((c) => io.observe(c));
    }

    // ── email copy ──
    const email = 'sihoon.cho.business@gmail.com';
    const copyBtn = root.querySelector<HTMLButtonElement>('[data-copy]');
    const feedback = root.querySelector<HTMLElement>('[data-copy-feedback]');
    const copyLabel = root.querySelector<HTMLElement>('[data-copy-label]');
    if (copyBtn && feedback) {
      let timer: number | undefined;
      const show = (msg: string) => {
        feedback.textContent = msg;
        feedback.classList.add('is-on');
        if (copyLabel) copyLabel.textContent = '완료';
        window.clearTimeout(timer);
        timer = window.setTimeout(() => {
          feedback.classList.remove('is-on');
          if (copyLabel) copyLabel.textContent = '복사';
        }, 2000);
      };
      const fallbackCopy = () => {
        try {
          const ta = document.createElement('textarea');
          ta.value = email;
          ta.setAttribute('readonly', '');
          ta.style.position = 'absolute';
          ta.style.left = '-9999px';
          document.body.appendChild(ta);
          ta.select();
          document.execCommand('copy');
          document.body.removeChild(ta);
          show('복사되었습니다!');
        } catch {
          show('복사에 실패했습니다. 직접 복사해 주세요.');
        }
      };
      copyBtn.addEventListener('click', async () => {
        try {
          if (navigator.clipboard && window.isSecureContext) {
            await navigator.clipboard.writeText(email);
            show('복사되었습니다!');
          } else {
            fallbackCopy();
          }
        } catch {
          fallbackCopy();
        }
      });
    }
  })();
</script>
